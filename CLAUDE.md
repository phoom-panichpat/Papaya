# Papaya — project guide (read this first)

Papaya is a casual **expense-splitting app for friend groups** (a lighter, more flexible Splitwise). This file is the single source of truth for any agent continuing the build. Read it fully before working.

---

## 1. What it is (the product model)

- **People-first, not trip-first.** The durable unit is the *group of people*, not a trip. Balances between people persist forever (no per-trip reset).
- **Expense-first capture.** The app opens ready to log an expense in seconds (what / how much / who). Organization is optional and happens after.
- **Recordings** = optional containers that group expense logs (a trip, a night out, a recurring group). Purely organizational — they never hold balances. An expense can also be **loose** (no recording). One recording can be the **live session** (its `REC` toggle on) — new expenses auto-file into it; only one live at a time.
- **People are the atom.** Global, persistent. A person is either a real account or a **token** (placeholder, no account). Everyone (including "You") is a person row.
- **The splitting engine** is the signature feature: an expense = a list of **items**, each with an amount + its own member set. One catch-all item ("the rest") auto-fills its amount (total − carve-outs) and defaults to everyone but is editable. Covers even-split, wine-for-some, someone's extra dish, people-who-left-early — all with one model.
- **Currency:** three layers, each inheriting: MAIN (user home currency, default THB, in Settings) → RECORD currency (optional per recording) → EXPENSE currency (inherits record). A foreign expense shows an editable exchange rate to the **recording's base currency**.
- **Settlement:** balances are pairwise between people, global. Global tab settles person-by-person; minimized "who pays who" is scoped inside a record (never route a payment between people who don't share a record).

Deeper design rationale lives in the agent memory file (`~/.claude/projects/-Users-hokkaido-Desktop-claude-Papaya/memory/papaya_v2_direction.md`) and in `design/` (approved wireframes → styled reference + token spec).

---

## 2. Current status

**This is v2, a full rewrite of a v1 prototype. Branch: `papaya-v2`.** Build phases:

| Phase | Status |
|---|---|
| 1. Foundation (schema, tokens, shell, auth) | ✅ done, committed |
| 2. Auth + onboarding | ✅ minimal (inline in App.jsx) |
| 3. Home screen | ✅ done, verified live |
| 4. Expense form (even-split capture) | ✅ done, verified live |
| 4b. "Split it up" carve-out engine | ✅ done, verified live |
| 5. Records / Recording detail / Create recording | ✅ done (Records tab later merged into Home — see revision) |
| 6. Settlement / Expense detail | ✅ done, verified live (30/30 math tests + browser E2E) |
| 6-rev. Phase-6 feedback rework (nav merge, People tab, settled lifecycle, archive) | 🔨 batches 1–2 done + verified live; 3–5 queued |
| 7. Settings / Friends | 🔨 People tab (roster/add/edit/remove) + Settings screen (home currency / profile / sign-out) DONE + verified; token merge/claim still ⬜ |
| 8-pre. Performance/data-layer refactor | ✅ P0 (in-repo tests) + P1 (optimistic settle) + P2 (scoped RecordSettleSheet load; cache/memo deferred) + P3 (archive refresh wiring) — **all built; P1/P2 still need Phoom's visual check**. See §8. → clear for Phase 8. |
| 8. Punch-list + deploy | ✅ **DEPLOYED 2026-08-05 → https://papaya-r1wp.vercel.app** (see §2b — real trip data now) |

**What works right now (verified live against Supabase):** sign in → **Home** merged feed (live recording bar under the header; search; recordings + loose expenses; settled expenses shown faded+struck; "+ Rec" new recording; FAB) → FAB opens expense form (keypad, currency dropdown, foreign exchange-rate card, payer + split pickers, "Split it up" carve-outs) → Save persists + Home refreshes. **People** tab = roster (All/Accounts/Placeholders + search + add/rename/emoji/remove). **Settlement** = Balances (You-centric direct pairwise, item-check toggle settle) + History (archived recordings + settled loose). Expense detail, recording detail, record-settle sheet all support toggle-in-place settle with faded+struck treatment. Archive/Unarchive routes records between Home and History.

**"Split it up" is now complete** (verified live: total → "Split it up" → add item "Wine" ฿300 → picker with Everyone/Clear chips assigns all 5 → the rest auto-recomputes to ฿700 → per-person peek correct (You ฿760, others ฿60) → Save persists as a loose expense, Home refreshes). Two wiring steps done: `itemPicker` now renders a `PeoplePicker`; `PeoplePicker` gained `onEveryone`/`onClear` chips. Also fixed: `setTargetAll`'s "Everyone" now selects all `localPeople` (previously only recording members + self, so it selected nobody on a loose expense).

**Phase 5 (Records / Recording detail / Create recording) is now complete** (verified live). New files: `src/screens/Records.jsx` (segmented Recordings|Loose control with per-view counts, search across both, "+ New recording", recording cards → detail, loose rows → expense-detail stub), `src/screens/RecordingDetail.jsx` (title + derived date range + logs/currency meta, "Who's in" party roster, ORANGE "Settle up this record" primary [stub → Phase 6] + "+ Add expense" secondary, per-expense settled indicators derived from `expense_item_members.settled_at` → "settled"/"open"/"N of M · Name open", log rows → expense-detail stub), `src/screens/CreateRecording.jsx` (name, optional "Who's in" via PeoplePicker, optional "Different currency" reveal + exchange rate, "Start recording now" toggle → clears other live recordings then inserts active; always adds self to members). **PeoplePicker has two multi-select shortcut modes** (a picker uses one, not both): (a) `onEveryone`/`onClear` chips = pare-down a known group, for the EXPENSE FORM (split/item pickers); (b) `suggestions` (`[{label, ids}]`) + `onAddPeople(ids)` chips = assemble a party from scratch, for CREATE RECORDING — it passes "From last time" (members of the most recent recording) + "Often" (people in ≥2 recordings), each a one-tap add that flips to a ✓ done state once its people are all selected. Design rationale (Phoom, 2026-07-13): Everyone/Clear is a splitting affordance and belongs only in the expense form; building a recording's roster wants recency/frequency suggestions, not "everyone". Verified: create "Chiang Mai trip" w/ Everyone → persists members + becomes the live REC session (took over from Busan, one-live enforced); add-expense from a recording detail forces that recording (chip "Recording · Seoul in June", inherits KRW, roster pre-filled) → saves → detail + Records both refresh (06→07 logs, date range extends).

**App.jsx navigation was refactored from a single `overlay` to an overlay STACK** (`stack` array + `push`/`popOverlay`) so nested pushes work (recording detail → add expense → pop back refreshed). `popOverlay(changed)` bumps `refreshKey` + reloads people; underlying mounted screens re-fetch on `refreshKey`. Records/RecordingDetail hit unbuilt screens (expense detail, settle-up) via an App-level `StubSheet`. `ExpenseForm` now accepts `forceRecordingId` to file into a specific recording instead of auto-detecting the live one.

**PHASE 6 REVISION IN PROGRESS (2026-07-14 — from Phoom's Phase-6 feedback pass).** Reworking Phase 6 per a batch of comments. Decisions locked with Phoom then built in batches. **Done + verified live so far (batches 1–2):**
- **NAV RESTRUCTURE (batch 1):** Home and Records were ~redundant, so **merged into one Home feed**; nav is now **Home / People / Settlement** (was home/records/settlement). New `src/screens/People.jsx` (roster: All/Accounts/Placeholders segment + search + add/rename/emoji/remove-with-FK-guard; token merge/claim still TODO). `Records.jsx` DELETED. `Home.jsx` rebuilt as the merged feed: live **recording bar** pinned under the header (glowing dot + REC + name → detail; the live indicator was moved OUT of the cards per D/b), search, "+ Rec" (new recording), the recordings+loose feed, FAB. `Settlement.jsx` gained **Balances | History** sub-tabs.
- **SETTLED LIFECYCLE (batch 2):** settling is now **direct toggle-in-place** everywhere (tap a person/transfer row → paid; tap again → un-paid) — reversibility-in-place is the misclick guard, backed by a short Undo toast (dropped the tick+commit step). Settled things **stay visible, faded + strike-through** (not disappearing) on Home feed, recording-detail logs, expense detail, and the record settle sheet. Fixed the record-sheet **pulsing bug** (all buttons flashed because `busy` dimmed them — removed busy-driven opacity). **`settled_at` is the sole source of truth**; we **stopped writing the `settlements` ledger** in settle flows (it was unused for balances; History derives from `settled_at`). `recordPayment`/`deletePayment` remain in `balances.js` but are now unused. **ARCHIVE (A/b):** new `recordings.archived_at` column (migration: `alter table recordings add column archived_at timestamptz;` — **applied to the live DB**). Recording detail has an **Archive/Unarchive** header action; archived records leave Home (`!r.archived_at` filter) and appear in **Settlement → History** (alongside settled loose expenses), tappable → detail → Unarchive. Rule: a fully-settled record does NOT auto-archive (living records, e.g. still-in-Korea) — archiving is a deliberate action.
- **Batch 3 adaptive pickers — DONE + verified live (2026-07-14, built by GLM 5.2 in opencode, Phoom visually verified loose + in-record).** New `src/lib/suggestions.js` extracts `computePeopleSuggestions(selfId)` (the "From last time" = most-recent-recording members + "Often" = high-frequency people logic), now **shared** by `CreateRecording.jsx` and `ExpenseForm.jsx`. Expense-form split + per-item pickers are now adaptive: **in-record** → "Everyone" (= record members) + "Clear"; **loose** → "From last time" + "Often" suggestion chips (no "Everyone"). Also fixed `setTargetAll` so "Everyone" selects the record's members (not the whole friends list). Note: GLM's "Often" scores by recordings **+ expenses** combined (slightly broader than the old "≥2 recordings" definition — harmless).
- **Batch 4a dual-currency in RECORD views — DONE + verified (2026-07-14, GLM 5.2 in opencode, Phoom visually verified).** New `src/components/DualMoney.jsx` (display-only: renders a passed-in primary money node, and when `homeAmount`/`homeCur` are given, stacks a smaller faded home-currency line beneath — Doto digits + greyed symbol, `--text-3/--text-4`). Applied to `RecordingDetail.jsx` log rows + `RecordSettleSheet.jsx` transfers; faded home line shows only when the record's `base_currency` ≠ home. Uses existing `toHome` (no new math). Layout locked: **stacked** (home beneath primary).
- **Still queued (batch 4 remainder):**
  - ~~**4c dual-currency in EXPENSE DETAIL**~~ **— DONE + verified (2026-07-14).** `ExpenseDetail.jsx` main amount + each "Who owes you" per-person row now use `DualMoney` (foreign → currency primary + faded home; home-cur → single figure). Old separate "≈ THB" hint removed. Reuses `DualMoney` + `toHome`, display-only. (Built by DeepSeek V4 Flash free-tier — the per-person rows were a follow-up miss, fixed in a second pass.)
  - ~~**4b dual-currency in the GLOBAL person sheet**~~ **— DONE + verified (2026-07-14).** `Settlement.jsx` person settle-sheet: each share row now shows home-currency primary + faded **native** currency beneath (only when native ≠ home). Inverse of the record pattern. Reuses `DualMoney` (its `homeAmount/homeCur` secondary slot is fed the native amount here — commented in-code). Top-level summary + per-person net cards stay home-only (they aggregate across currencies). **→ ALL OF BATCH 4 COMPLETE.**
- **Still queued:** batch 5 settle-up button reproportion (D/a: ~75–80% width inline, "+ Add expense" shrinks to a square "+").
- **Note:** live seed has accumulated test junk (empty "ok"/"olive" recordings, stray loose test expenses) — wipe + re-seed for a clean demo.

**Phase 6 (Settlement + Expense detail) is now BUILT** (compiles clean; core math unit-tested 24/24; live-auth UI pass still pending — I could not sign in, entering a password to authenticate is a safety line I won't cross). The three stubs are replaced. New files:
- `src/lib/balances-core.mjs` — **pure** money math, no Supabase, so it's unit-testable in plain Node (`node scratchpad/test-balances.mjs` style). Exports: `toHome` (2-hop currency convert: expense→record-base→home), `buildContributions` (one debt atom per person-per-item; the payer never owes their own share; carries `home`/`base`/`native` amounts + `settled`), `pairNet`, `netByPerson(contribs, scope, key)` (`key`='home' global / 'base' in-record), `minimizeTransfers` (greedy fewest-payments), `statusForExpense` (extracted from RecordingDetail so both screens agree).
- `src/lib/balances.js` — re-exports the core + adds IO: `loadSettlementData`, `settleShares`/`unsettleShares` (set/clear `settled_at` — the single source of truth for outstanding), `recordPayment`/`deletePayment` (history ledger + Undo).
- `src/screens/ExpenseDetail.jsx` — read-only expense: title/amount (+ base-currency ≈ if foreign), paid-by/when/recording meta, "Made up of" item breakdown (the rest + carve-outs, member chips, per-head), "Who owes X" per-person rows with settled/open pills (settled = struck-through/greyed), Edit (→ ExpenseForm edit mode) + Delete (confirm; cautions if settled portions exist; cascade-deletes items/members).
- `src/screens/Settlement.jsx` — the global tab (replaces the Placeholder). You-centric **direct pairwise** balances: owed-to-you / you-owe summary + per-person cards (green owes-you / ochre you-owe), tap → **person-detail bottom sheet** with **item-check partial settle** (tick the shares that got paid, live "remaining" net readout, deliberate "Mark N settled" commit, Undo snackbar 6s). Never routes between strangers — it's strictly You↔each person.
- `src/screens/RecordSettleSheet.jsx` — the "Settle up this record" sheet (RecordingDetail `onSettle`). Scoped to the record's party: `minimizeTransfers` → "A pays B ฿X" rows in the record's base currency, per-transfer "Paid" (all-or-nothing) + "Mark all as paid" (confirm) + Undo; "This record is settled up" empty state.

**Money model decisions locked in this phase:** (1) `settled_at` on `expense_item_members` is the **single source of truth** for outstanding — settling sets it, Undo clears it; the `settlements` table is a parallel **history ledger only** (never subtracted again, so no double-counting). (2) Global tab settles in **home currency**; in-record settle uses the **record's base currency** (`.base` amount, one conversion hop). (3) Editing an expense (ExpenseForm `editExpenseId`) updates the row then **deletes + rebuilds** its items/members — which resets any settled status on that expense (acceptable: changing the split invalidates prior settlement). (4) `ExpenseForm` gained `editExpenseId` (reconstructs even-split vs sliced state from stored items). App.jsx: `openExpense(id)` pushes `expenseDetail`; RecordingDetail `onSettle` pushes `recordSettle`; Home/Records/RecordingDetail all route expense taps to the real detail now.

**Settled/open palette (was flagged for this pass): now real tokens** in `index.css` — `--settled #4E7A55` (muted olive) + `--open #B07A2E` (muted ochre, quieter than the `--accent` orange). RecordingDetail's pill updated to use them (dropped the inline fallback).

**LIVE-VERIFIED E2E (2026-07-13, Phoom signed in).** Drove all three surfaces in the browser against the seed. Two gaps Phoom caught + fixed, and one real bug found + fixed during testing:
- **Home → recording detail** (Phoom: "cannot go to record detail by clicking record in home"): Home's accordion only expanded inline with no route to the detail (loose rows had "Open detail →", recordings had nothing). Added an **"Open recording →"** affordance to the expanded recording card (mirrors the loose-row pattern); `Home` gained an `onOpenRecording` prop → App pushes `recordingDetail`. Verified: opens the record detail.
- **Settle from the expense detail** (Phoom: "no settle button, can only settle through record screen"): the detail's per-person "Who owes X" rows are now **tap-to-settle** (tick open people → deliberate "Mark N settled · ₩X" commit → 6s Undo snackbar; settled rows show struck-through + SETTLED, not tappable). This is the only way to settle a **loose** expense per-item, and a faster path than the record sheet. Verified: ticked You on KTX → committed → persisted SETTLED.
- **RECORD SETTLE REDESIGN (real bug → design change).** The record sheet originally showed `minimizeTransfers` (fewest-transfers). Testing exposed that **minimization is fundamentally incompatible with per-item settled tracking**: a minimized transfer routes debt indirectly (e.g. "Sofia pays Rui ₩56k" where part of Sofia's net is actually owed to a third person), so marking it "Paid" can only settle Sofia↔Rui's *direct* items — leaving a residual and, worse, a stuck state (clicking Paid again settles nothing). Since per-item `settled_at` is what powers the per-expense settled indicators, **item-backing wins**: replaced `minimizeTransfers` with a new **`directTransfers(contribs, scope, key)`** — direct pairwise net per (debtor→creditor) pair, each transfer carrying the exact item `shares` it settles. Within one record's connected party there's no stranger-routing concern anyway. **Tradeoff: the record view is no longer guaranteed "fewest transfers"** (only differs when a record has multiple payers; single-payer records are unchanged). Header label changed "fewest transfers" → "who owes who". `minimizeTransfers` is kept in the core (unused now, retained for reference). Verified: Busan → You pays Rui ₩22,000 (= 39,333 KTX − 17,333 Raw fish) + Sofia pays You ₩17,333.33, per-transfer Paid + Undo work, no stuck state.
- **Money rendering fix:** the settle screens concatenated the currency symbol INTO the Doto `.money` span, so the ₩/฿ glyph rendered in dot-matrix and overlapped the digits. Added a small `Money` component (greyed grotesque symbol + Doto digits, digits optionally tinted) in `Settlement.jsx` + `RecordSettleSheet.jsx` — matches the global money rule. Verified clean.
- Global Settlement tab verified: OWED-TO-YOU/YOU-OWE summary + per-person direct pairwise (all converted to home THB), person sheet aggregates a person's open shares **across records + loose** with pre-ticked item-check + "settling +฿X / settles up fully" + commit.

`vite.config.js` now honors `PORT` (preview-harness autoPort) + `.claude/launch.json` `"autoPort": true`. Math tests now **30/30** (added `directTransfers` two-payer case + no-strand assertion). **Seed note:** live testing left **KTX tickets (Busan) fully settled** (You + Sofia) — a coherent partial-settle state, not corruption. There is no un-settle UI outside the Undo snackbar, so to get a pristine seed either wipe + re-seed or clear `expense_item_members.settled_at`.

Commits on `papaya-v2`: `8725d11` (foundation/Home/expense form), `c2a23f2` (cleanup + form refinements), `77e7b08` (Phase 4b Split it up + Phase 5 Records/detail/create + overlay-stack nav + suggestion chips), `be6bf25` (Phase 6 Settlement/detail + Phase 7 People/merge/settings/history — the whole span through Unit 5), `f43ab53` (Phase 8-pre P0 in-repo guardrail tests), `44b4fac` (Phase 8-pre P1 optimistic settle + P2 scoped record load), `8f00f59` (P3 archive-refresh + detail-screen UX pass: load-jank, expense-archive-pops-out, un-settle unchecked+red, snackbar removed, archive loads-then-pops with retry + no button-flip — all Phoom-visually-checked 2026-07-17), `3e1feaf` (REC live-session switch: arm-then-confirm toggle replacing the modal, 5s expiry + tap-elsewhere cancel, optimistic switch, Rec label live-card-only — Phoom-visually-checked 2026-07-17), `b1dabe9` (P4 load-jank + re-fetch pass: ExpenseForm/CreateRecording/RecordSettleSheet parallelized + gated, tab screens lazy-mount-and-stay-mounted, Settings zero-network + homeCurrency lifted to App, settle-row sort stable, currency-row tap fixed — all Phoom-visually-checked 2026-07-17), **`c0da7ab` (currency invariant C1: `expenses.home_rate` pins each expense's native→home rate at creation; `toHome` reads the pin; `base` derived from it so in-record settlement can't drift either; migration + backfill applied live; tests 55/55 — Phoom-visually-checked 2026-07-17, repro fixed)**, **`3ef2839` (C2b: Settlement balances frozen per native currency — `pairNetByCurrency`; per-person cards render one frozen line per currency + a faded home total; person-sheet rows flipped to native-primary; fixes people vanishing when cross-currency debts home-netted to ~0; tests 59/59 — Phoom-visually-checked 2026-08-05)**. **`e203454` (currency ERA model, unit 1 = 1a data foundation + 1b visible eras + 1c hard typed switch: `home_currency` on expenses AND recordings, inherited from containers; `eraFor`; per-era display everywhere; home-per-era on the global tab, superseding C2b's frozen-native cards; typed confirm in Settings that never asks for a rate. Migration applied live; tests 73/73 with the original 59 unchanged; Claude-verified live in the browser incl. a real THB→USD→THB switch — 2026-08-05).** **`2753005` (§10 unit 2 expense quick wins: PeoplePicker "Picked" strip + You-first alphabetical order with a deliberately non-reflowing list; item pickers scoped to Who's-in via `memberLabel`; chain "+ same people" items; collapsible "Made up of" + distinct-people per-head fix. Display-only — tests unmoved at 73/73; Claude-verified live in an isolated anonymous account — 2026-08-06. Pushed + deployed 2026-08-06.)** **`d48f49b` (unit 4 hardware back: `src/lib/backstack.jsx` + `useBackLayer` wired into ~20 dismissible layers; depth-reconciling history sync; `BACK_LEVEL` ordering; tab-back→Home→exit; `active` gating for kept-mounted tabs. Tests 94/94 untouched; Claude-verified in-browser, **awaiting Phoom's real-device pass** — 2026-08-06.)** **⚠️ Working tree note: `src/App.jsx` holds an UNCOMMITTED dev-only "continue as guest" button on the auth screen (anonymous sign-in, `import.meta.env.DEV`-gated) — not part of unit 1; Phoom's call whether it lands. It must never reach production unguarded.** ⚠️ P1+P2's optimistic-settle internals were committed before a dedicated visual pass (though the later detail-screen checks exercised the settle sheets) — sanity-check settle/un-settle once more before deploy. **Pushed + deployed — see §2b.** ⚠️ The live DB was wiped + re-seeded 2026-08-05, but **it now holds Phoom's REAL Korea-trip data — see §2b. Do not propose wiping it again.**

---

## 2b. 🔴 DEPLOYED — AND PHOOM IS USING IT FOR REAL (2026-08-05)

**Live: https://papaya-r1wp.vercel.app** — Phoom installed it to his phone home screen and took it on a **Korea trip**. Read this before touching anything that writes to the DB.

- **⚠️ THE LIVE DATABASE NOW HOLDS REAL TRIP DATA, NOT SEED DATA.** Everything before this was disposable test data that could be wiped and re-seeded. That is over. Treat migrations, backfills, and any destructive SQL as production changes: propose them, explain the blast radius, and let Phoom run them. **Never suggest "wipe + re-seed" again** without asking.
- **Vercel project `papaya-r1wp`** (separate from v1's project — v1 is untouched). **Production branch = `papaya-v2`**, so *any push to `papaya-v2` deploys straight to his phone.* There is no staging. Say so when proposing a push.
- Env: `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` set in Vercel (Production+Preview), pointing at the **v2** Supabase project `alvmrh…`. Vite inlines them at build time → **changing an env var requires a redeploy**, not just a save.
- **v1 is dead, don't be confused by it:** `main` holds v1, which **hardcodes its Supabase URL in `src/App.jsx`** (ignores env vars), and its Supabase project `hahahp…` no longer responds (paused/deleted). If a deploy ever shows the dark UI + "Failed to fetch", it built `main`.
- PWA: `public/manifest.json` + generated flat icons (accent orange on cream) + `apple-touch-icon` — commit `02af6ed`. `vercel.json` handles SPA rewrites.
- **Known gaps in real use, already told to Phoom:** (1) **no offline support** — every action is a live Supabase call, so no signal = cannot log; (2) **no backup/undo** on free-tier Supabase (he was asked to export CSVs before flying); (3) **anonymous sign-in is ON** in Supabase — it was enabled so Claude could drive the UI himself via the DEV-only "continue as guest" button. If Phoom turns it off, Claude loses live-UI verification and Phoom resumes doing all visual checks.

---

## 2b. 🔴 DEPLOYED — AND PHOOM IS USING IT FOR REAL (2026-08-05)

**Live: https://papaya-r1wp.vercel.app** — Phoom installed it to his phone home screen and took it on a **Korea trip**. Read this before touching anything that writes to the DB.

- **⚠️ THE LIVE DATABASE NOW HOLDS REAL TRIP DATA, NOT SEED DATA.** Everything before this was disposable test data that could be wiped and re-seeded. That is over. Treat migrations, backfills, and any destructive SQL as production changes: propose them, explain the blast radius, and let Phoom run them. **Never suggest "wipe + re-seed" again** without asking.
- **Vercel project `papaya-r1wp`** (separate from v1's project — v1 is untouched). **Production branch = `papaya-v2`**, so *any push to `papaya-v2` deploys straight to his phone.* There is no staging. Say so when proposing a push.
- Env: `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` set in Vercel (Production+Preview), pointing at the **v2** Supabase project `alvmrh…`. Vite inlines them at build time → **changing an env var requires a redeploy**, not just a save.
- **v1 is dead, don't be confused by it:** `main` holds v1, which **hardcodes its Supabase URL in `src/App.jsx`** (ignores env vars), and its Supabase project `hahahp…` no longer responds (paused/deleted). If a deploy ever shows the dark UI + "Failed to fetch", it built `main`.
- PWA: `public/manifest.json` + generated flat icons (accent orange on cream) + `apple-touch-icon` — commit `02af6ed`. `vercel.json` handles SPA rewrites.
- **Known gaps in real use, already told to Phoom:** (1) **no offline support** — every action is a live Supabase call, so no signal = cannot log; (2) **no backup/undo** on free-tier Supabase (he was asked to export CSVs before flying); (3) **anonymous sign-in is ON** in Supabase — it was enabled so Claude could drive the UI himself via the DEV-only "continue as guest" button. If Phoom turns it off, Claude loses live-UI verification and Phoom resumes doing all visual checks.

---

## 3. How to run

```bash
npm install
npm run dev      # Vite dev server, http://localhost:5173
npm run build    # production build (use to verify compile after changes)
npm test         # money-core guardrail tests (tests/balances-core.test.mjs) — must stay 136/136
```

- Env: `.env` holds `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` (gitignored; template in `.env.example`). The Supabase project is fresh and the v2 schema is applied.
- **Email confirmation is OFF** in the Supabase project (required — otherwise signup can't log in). If auth breaks, re-check Auth → Sign In/Providers → Email → "Confirm email" is off.
- Empty account? Home's empty state has an **"add demo data"** button (`src/lib/seed.js`) that seeds realistic recordings/expenses/people.
- Preview server config: `.claude/launch.json` (name `papaya`).

---

## 4. Tech stack & repo structure

React 18 + Vite, plain JSX (no TypeScript), Supabase (auth + Postgres). No router lib — navigation is state-based in `App.jsx`.

```
src/
  App.jsx              # auth gate, 3-tab shell (home/people/settlement), overlay stack, loads `people`
  main.jsx             # entry; imports index.css
  index.css            # ALL design tokens (CSS vars incl. --settled/--open) + fonts + keyframes
  lib/
    supabase.js        # client from env vars
    format.js          # currencySymbol / formatMoney / padIndex
    seed.js            # dev demo-data seeder
    balances-core.mjs  # PURE money math (no Supabase; node-testable): buildContributions,
                       #   pairNet, netByPerson, directTransfers, minimizeTransfers, statusForExpense, toHome
    balances.js        # re-exports the core + IO: loadSettlementData, settleShares/unsettleShares
                       #   + summaries: createSummary/revertSummary/settleTransfer/loadSummaries
  screens/
    Home.jsx           # merged feed (recordings + loose + recording bar + search + new-recording + FAB)
    ExpenseForm.jsx    # expense capture (even-split + "split it up" carve-outs; editExpenseId edit mode)
    ExpenseDetail.jsx  # read-only expense + collapsible "Made up of" + per-person toggle-settle + Edit/Delete
    RecordingDetail.jsx# title/roster/logs + Settle-up + Add-expense + Archive/Unarchive
    CreateRecording.jsx# name, members (suggestions + Clear), optional currency, start-now
    People.jsx         # roster: All/Accounts/Placeholders + search + add/rename/emoji/remove
    Settlement.jsx     # Balances (direct pairwise + person item-check sheet) | History (archived + settled loose)
    RecordSettleSheet.jsx # scoped "who owes who" direct-pairwise, toggle-in-place paid/unpaid
    backstack.jsx      # hardware/browser back: useBackLayer(open, onBack, level) + BackStackProvider.
                       #   Depth-reconciling history sync; BACK_LEVEL orders tab < screen < sheet.
  components/
    PeoplePicker.jsx   # reusable bottom-sheet: search-that-creates; pinned "Picked" strip (list never
                       #   re-flows); You-first alphabetical; Everyone/Clear OR suggestion chips; memberLabel
                       #   names the primary group ("In this recording" / "In this expense")
    SaveError.jsx      # transient "couldn't save — showing latest saved state" notice (optimistic-UI failure path)
tests/
  balances-core.test.mjs # money-core guardrail suite (plain Node; `npm test`, 136/136)
schema.sql             # v2 Postgres schema (apply to a clean DB)
design/                # TOKENS.md (design spec) + reference .dc.html mockups
CLAUDE.md              # this file
```
(Deleted: `Records.jsx` — merged into Home in the Phase-6 revision.)

---

## 5. Data model (schema.sql)

Single-user model — **every table has `owner_id` and RLS is a plain `owner_id = auth.uid()` check.** This is deliberate and non-recursive (the v1 bug was recursive RLS policies; never reintroduce cross-table policy lookups).

- `profiles` (extends auth.users; `home_currency` default THB)
- `people` (global contacts/tokens; `is_token`, `is_self`, `linked_profile_id`, **`merged_into_id`** = non-destructive person→person alias pointer for token→account merge; a "self" person is auto-created on signup by the `handle_new_user` trigger)
- `recordings` (`base_currency`, `exchange_rate`, **`home_currency`** = the home currency this record was created under, its **era**, fixed at creation and inherited by every expense in it; `is_active`, **`archived_at`** null=active/on-Home, set=archived/in-History) + `recording_members`
- `expenses` (`recording_id` null = loose; `paid_by` → people; `currency`, `exchange_rate` = expense→record-base, now only a pre-fill/edit default, **`home_rate`** = this expense's OWN native→home rate **pinned at creation — the single thing every balance converts through**, so a record's currency can never retroactively rewrite a debt; **`home_currency`** = the currency `home_rate` converts INTO, i.e. this expense's **era** — so a later home-currency change can't mislabel an old pin; `archived_at`)
- `expense_items` (`is_rest` catch-all + carve-outs; `amount`, `label`) + `expense_item_members` (`settled_at` = **the single source of truth for outstanding** — set = paid)
- `settlement_events` (**append-only log**; a summary IS an event, so a revert appends `summary_reverted` rather than erasing. Nothing here affects a balance) + `summary_transfers` (the live obligations a summary creates: `settled_at` + `superseded_by`; **`home_amount` is pinned, not derived**) + **`expense_item_members.summary_id`** (the freeze: set = closed by a summary, a third state distinct from settled). The old `settlements` ledger is **dropped**.

Balances are **derived client-side** from items + members' `settled_at` **plus any live `summary_transfers`** — a debt is OPEN while it is neither settled nor superseded, the same rule for a share and for a transfer (no balance column), and every `person_id` is **resolved through `merged_into_id`** in `buildContributions` before netting (so a merge/un-merge instantly re-routes all past splits). Composite-key tables (`expense_item_members`, `recording_members`) have no `id` column. **All migrations are applied to the live DB** (`recordings.archived_at`, `expenses.archived_at`, `people.merged_into_id`, `expenses.home_rate` + its backfill) and are reflected in `schema.sql` for clean applies.

---

## 6. Design system (source of truth: `design/TOKENS.md` + `src/index.css`)

**Aesthetic:** warm Swiss/Scandinavian editorial minimalism, flat, light mode, retro-instrument accents (Teenage Engineering / Braun / Dieter Rams). Airy, hairlines not heavy borders.

- **Fonts** (Google Fonts, in index.css): **Instrument Sans** (grotesque UI), **IBM Plex Mono** (labels/meta/legends, UPPERCASE letter-spaced), **Doto** (ALL money digits — global rule; the currency symbol is grotesque, greyed).
- **Palette (CSS vars):** `--bg #F3F0E9` (warm cream), `--surface #FCFAF5`, text `--text #1D1B16` / `--text-2` / `--text-3` / `--text-4`, `--hairline #E4E0D3`, **`--accent #E8622A`** (burnt orange — ONE element per screen only: the live REC / primary action). Flat: no gradients/shadows.
- Radii: cards 18, sheet 22, toggle 14. Motion easing `cubic-bezier(0.2,0,0,1)`, 110–280ms. Helpers: `.money`, `.mono`, `.legend`.
- **Reproduce the look by matching the styled reference in `design/`, not by re-deriving it.** Dark mode is deferred (derive later via token swap).

---

## 7. Conventions, patterns & gotchas

- **Navigation:** `App.jsx` holds `tab` (**home/people/settlement** — Records was merged into Home, see §2 revision), an overlay **`stack`** (array of full-screen pushed screens — ExpenseForm, CreateRecording, RecordingDetail — rendered on top of the tab, last on top), a `stub` (App-level StubSheet for not-yet-built targets like expense-detail/settle), and `refreshKey` (bump → mounted screens reload). Push with `push(o)`; each pushed screen gets `onClose`/`popOverlay(changed)` — `changed=true` pops + bumps refreshKey + reloads people. Screens re-fetch on the `refreshKey` prop so an underlying screen refreshes when an overlay above it saves.
- **RLS scopes queries automatically** to the owner; you never filter by `owner_id` in selects, but you MUST set `owner_id` on every insert.
- **After creating tokens/people**, refresh the `people` list (App reloads on overlay close).
- **Browser testing:** controls are `<div onClick>` not semantic buttons; coordinate clicks are unreliable. Drive via `javascript_tool` DOM `.click()` (e.g. find the FAB by its SVG path, click `.closest('div')`). React controlled inputs need real typing or the native value-setter + `input` event.
- **Permissions:** `.claude/settings.local.json` (gitignored) is set to `acceptEdits` + a broad Bash allow-list — routine git/npm/bash/edits run without prompting. Still confirm outward-facing/irreversible actions.
- Commit only meaningful checkpoints. Commit messages end with `Co-Authored-By: Claude <noreply@anthropic.com>`. Never commit to `main` (v1 is there / deployed).

---

## 8. Immediate next task: Phase 8-pre (performance/data-layer refactor) → Phase 8 (deploy) — fresh session recommended

**Phase 7b state:** Units 1 ✅, 2 ✅, 3 ✅ (alias-merge foundation; `merged_into_id` migration **applied to live DB** 2026-07-15), 4 ✅ + 4b ✅ (merge UI, generalized + verified live), **5a ✅ + 5b ✅ — UNIT 5 COMPLETE, verified live 2026-07-17.** **Phase 7c COMPLETE** (7c-1 settled events, 7c-1a sub-nav/search, 7c-1b ticking symmetry, 7c-2 person detail, 7c-2b + 7c-3 + 7c-3b archive gating/refinements, 7c-4 History legibility — all verified live). Migrations applied live: `recordings.archived_at`, `expenses.archived_at`, `people.merged_into_id`. **→ Phase 7 (all of 7b + 7c) is now DONE. The v1 feature set is effectively frozen; next is the Phase 8-pre performance refactor, then Phase 8 punch-list + deploy. Audit-trail (append-only settle log + comments) stays parked for its own design session — see the ⚠️ block below.**

**Unit 5 = 5a (hygiene/unmerge/display resolution) + 5b (in-context merge). BOTH DONE + verified live 2026-07-17.**

**5a ✅ DONE + verified live (2026-07-17, GLM, decision: merge = permanent `mergePerson` alias, option (a)).** Two things shipped: **(1) unmerge** — `PersonDetail.jsx` shows an **"Unmerge" text button** in the header (left of Edit) whenever ≥1 person's alias resolves to this person; tapping opens a "Merged into {name}" sheet listing each merged-in person with an Unmerge action → `unmergePerson(id)` → `onChanged()` (new App prop, mirrors People) + local `load()`; person pops back out, past shares re-route back automatically. Fulfils the "you can undo this later" promise the merge confirm sheet already made. **(2) merged-token display hygiene EVERYWHERE** — a merged token must never appear as a pickable option OR a stray/duplicate chip. Fixed across **five id sources** (the leak was NOT just one filter — every place a raw stored/roster `person_id` is read must resolve through the alias): **`ExpenseForm.jsx`** — `localPeople` filters `!merged_into_id`; edit-mode reconstruction + new-expense path both resolve `recording_members`/`paid_by`/item-member ids through `resolveAlias`. **`CreateRecording.jsx`** — same `localPeople` filter + edit-mode member resolution. **`suggestions.js`** (`computePeopleSuggestions`, shared by ExpenseForm loose-picker + CreateRecording) — fetches `people`, builds `aliasMap`, resolves every id (From-last-time / Often) so merged tokens can't ride in via a suggestion chip. **`RecordingDetail.jsx`** — `nameOf` + the "Who's in" roster (`recording_members`) resolve + dedupe through the alias. **`ExpenseDetail.jsx`** — `person()` resolves; the `perPerson` "Who owes" loop groups raw member rows by **canonical** person (divides per-head by distinct canonical count, keeps RAW rows in `allShares` so settle writes still hit real DB rows, skips payer's canonical id); "Made up of" chips render distinct canonical members. **Key lesson for future work: alias resolution is a cross-cutting DISPLAY concern — any new screen that renders a person from a raw stored/roster id must resolve it through `buildAliasMap`/`resolveAlias` (both exported from `../lib/balances`), not just filter.** Note: Home feed + global Settlement person cards were spot-checked and were clean (they derive from `buildContributions`, already resolved); if a stray token ever appears there it's the same class of bug.

**5b ✅ DONE + verified live (2026-07-17, GLM, built iteratively over several passes).** In-context merge inside the "who's in" pickers of **edit-expense** + **edit-record**, so identities get fixed without leaving the flow. **Design evolved with Phoom mid-build (important — the first cut was rejected):** the original bespoke "Fix identities / Swap" two-step (list tokens → tap Swap → pick target) was **too confusing**; Phoom's call = **reuse the exact People-tab merge UX** instead. Final shape: `PeoplePicker` gained an `onMergePeople(idsToMerge[], targetId)` prop → when set (EDIT mode only), a **"Merge"** button (same wording as People) opens **merge mode = checkbox rows on every person + the same legality rules copied from People** (`selfSelected`/`accountSelected`/`autoTarget`, `realSelectedCount<=1`, realLocked fade, self target-only) → the **exported `MergeConfirmSheet`** (now `export`ed from `People.jsx`, keep-picker when all placeholders) → on confirm calls `onMergePeople`. `ExpenseForm`/`CreateRecording` each have a `mergePeople(ids, targetId)` that loops `mergePerson`, then **canonicalizes local selection** (`splitIds`/`restMembers`/each item's members/`paidBy`/`members`/`whosIn` remapped token→target, dropped from `localPeople`). Undo = the PersonDetail "Unmerge" button (no snackbar). **Also fixed in 5b:** (a) **self reachable in People** — the self row now opens PersonDetail (was un-tappable), so a token merged into "You" is unmergeable; (b) **`onMergePeople` is on every multi-select picker in edit mode** (main split, Who's-in, the rest, items) — not just one.

**5b follow-on — persistent independent "Who's in" roster in split-up mode (verified 2026-07-17).** In sliced ("Split it up") mode there's now a persistent **"Who's in" row** above "the rest". It's backed by **independent `whosIn` state** (NOT a derived union): a person stays in the expense until explicitly removed from Who's in — bucket membership (the rest / items) is a **subset**. A `useEffect` maintains the invariant `whosIn ⊇ (restMembers ∪ all item members)` by **only adding**, so removing someone from the rest/an item leaves them **parked** in Who's in (owing nothing) — the "Marco only drinks wine, but wine isn't logged yet" case. `toggleWhosIn` remove = full exit (whosIn + rest + every item); add defaults them into the rest. **⚠️ Known limitation (flagged, not fixed):** a parked person (in `whosIn`, no bucket) owes nothing, so `save()` (persists restMembers + items only) won't store them — on reopen, Who's in = whoever had a share. Persisting zero-share participants needs a new `expense_members`/participants table (future, if it ever matters).

Then the audit-trail phase (its own design session — see the ⚠️ block below) and **Phase 8** (punch-list + deploy). — The Phase-6 revision batches below are ALL DONE (kept for reference): — The Phase-6 revision batches below are ALL DONE (kept for reference):

- **Batch 3 — adaptive people-pickers** (in `ExpenseForm.jsx` + `PeoplePicker.jsx`): for an expense **inside a record**, the split/item picker's "Everyone" chip must select the **record's members** (not the whole friends list) + keep "Clear". For a **loose** expense (no record), drop "Everyone" and instead show **"From last time"** + **"Often"** suggestion chips (same as CreateRecording — reuse/extract its suggestion-computing logic; the `PeoplePicker` already renders `suggestions`+`onAddPeople`). This reverses the old "suggestions only in create-recording" split. (Punch-list #7-old was Everyone=members; the loose-expense "selects nobody" bug is why it was changed to all-people — the fix is: members-when-in-record, suggestions-when-loose.)
- **Batch 4 — dual-currency display:** in a **foreign-currency record**, expense rows (recording detail + expense detail) show the record currency as primary + a small **faded home-currency** amount below. In the **global Settlement** it's the inverse: card total = **home only**; inside the **person settle sheet**, each item row shows home primary + a small **faded native** currency below. (`Money` helper components already exist in Settlement/RecordSettleSheet.)
- **Batch 5 — design polish (D/a):** the recording-detail **"Settle up this record"** button is too long/wide — make it inline at ~**75–80% width** with **"+ Add expense"** shrunk to a small square **"+"** beside it. (D/b recording-bar + more-pronounced live indicator already done in batch 1.)

**Phase 7 — Settings screen DONE + verified (2026-07-15).** New `src/screens/Settings.jsx` (pushed overlay): home/main currency picker (reuses the CURRENCIES list; writes `profiles.home_currency` ONLY — never touches record exchange rates), profile (email + edit self person's name/emoji → `people` `display_name`/`avatar_emoji`), sign-out (`supabase.auth.signOut()`). Home gear wired: `onOpenSettings` now `push({type:"settings"})` (was `setStub`). Fixes in the same pass: real cog icon (Home gear was sun-like radial rays); People self-row now shows the real `display_name` (was hardcoded "You"); App clears the overlay `stack`/tab/stub on sign-out so re-login lands on Home.

**PHASE 7b — RECONCILIATION + EDIT-RECORD PLAN (locked 2026-07-15 with Phoom).** Direction decision: the app WILL become multi-user eventually, but we finish single-user features now and **design everything multi-user-aware** (person is the global atom; identity resolves through a pointer; settlement in the record's shared currency — already true; no hardcoded self assumptions). Full "friends with their own accounts join a shared record and self-claim their token" = a deliberate FUTURE multi-user phase (needs shared-group RLS/invites; the current single-user `owner_id = auth.uid()` RLS forbids cross-user reads). 5 ordered units:
1. ✅ **DONE — Fix Edit-expense (overlay stacking).** Overlays had hardcoded z-index (ExpenseDetail 40 > ExpenseForm 30), so a form pushed on top of the detail was hidden behind it → "Edit did nothing." Fixed in `App.jsx`: the `stack.map` now wraps each pushed screen in a positioned container with `zIndex: 50 + i` (its own stacking context), so push order always wins. Verified: Edit opens the pre-filled form.
2. ✅ **DONE + verified — Edit-record screen** (GLM). `CreateRecording` takes `editRecordingId`: pre-fills name/currency/members, UPDATEs the row + diffs `recording_members` (adds new, removes dropped), hides "start recording now" in edit mode; RecordingDetail header has an **Edit** button → App pushes `createRecording` with the id. Also done in the same pass: **batch-5 settle-button reproportion** (RecordingDetail: "Settle up this record" is now inline primary `flex:1` beside a 48×48 square **"+"** add-expense). Form input `id`/`name` warning — no separate fix needed; the real cause was Unit-1 overlay stacking.
3. ✅ **DONE (built + 19/19 node tests + build clean; ⚠️ needs the `merged_into_id` migration run before it works live) — Alias-merge foundation** (Claude Code). Non-destructive `people.merged_into_id` person→person pointer. `balances-core.mjs`: new `buildAliasMap` + `resolveAlias` (chain-following A→B→C, cycle-guarded); `buildContributions` now resolves payer + every member id through the alias, **dedups aliases that land on the same person within one item** (divisor counts distinct people; a merged pair re-splits), and carries `settleKeys` = the ORIGINAL member rows each contribution stands for. Elegant choke-point: only `buildContributions` resolves — all downstream netting/transfers become canonical for free. `balances.js`: `loadSettlementData` now also loads `people`; added `mergePerson`/`unmergePerson` IO (set/clear the pointer). Consumers updated to settle via `settleKeys` (Settlement.jsx, RecordSettleSheet.jsx, directTransfers) so DB writes still hit real rows after a merge. Self-debt (merged member == payer) washes to zero via existing net math. Tests cover: baseline unchanged, redirect, self-wash, overlap-dedup + dual settleKeys, chain/cycle, dedup-settled-status. 🔴 Difficult → built in Claude Code.
4. ✅ **DONE + verified live — Merge selection UI in People** (GLM). "Merge" button beside "+ Add person" → merge mode: checkboxes on every row, selection is a `Set` in state so it **persists across the All/Accounts/Placeholders tabs**; confirm sheet; `mergePerson()` per non-target; roster hides people with `merged_into_id` set.
   - **4b — spec fix (Evie's miss, caught by Phoom in visual test).** The original spec required "≥1 placeholder + exactly 1 account", but **this is a single-user app: `self` is the ONLY non-token person** (`handle_new_user` inserts it with `is_token=false, is_self=true`) and GLM's code excluded self as a target → **zero valid targets, Merge button permanently disabled.** Fix = **generalize the model**: merge now means *"these people are really ONE person — keep this one"*, not "token→account". Target = **most-real identity checked, precedence `self > account > placeholder`**; if all checked are placeholders the confirm sheet asks **"Which name do you want to keep?"** (tappable list). Legal selection = **at most one real identity** (self/account) checked; other real-identity rows fade ~0.4 + become un-checkable ("realLocked"); placeholders always checkable. `mergeEnabled = N>=2 && isLegal`. Self is checkable but **can only ever be a target, never merged away** — that guard (not the feature) is the point: it prevents burying your own identity behind a token. Verified live: placeholder+placeholder merge w/ keep-picker, placeholder+You auto-target, Settlement combines debts under the kept name, Cancel exits clean.
   - **Design note (Phoom asked "why merge myself?"):** deliberate self-tokens are rare — the only real case is an accidental duplicate ("Phoom" added to a roster when already in it as You). The multi-user *claim* case does NOT apply to self-merge: a friend's token for you lives in THEIR data (per-user RLS), so they merge it into your account on their side; you never merge yourself. Keep self-as-target as a quiet safety valve, don't feature it. The everyday merge UX belongs in **Unit 5 (in-context swap)** — Phoom's own instinct.
   - **⚠️ KNOWN GAP → fold into Unit 5:** only `People.jsx` filters `merged_into_id`. `App.jsx` loads ALL people (`select("*")`) and passes the raw list everywhere, so **merged-away tokens still appear as pickable options in ExpenseForm / PeoplePicker / CreateRecording**. Balances resolve correctly regardless (roster-hygiene leak, not a money bug).
5. **In-context merge** — ✅ **COMPLETE (5a + 5b, verified 2026-07-17).** 5a = roster hygiene + unmerge + read-screen display resolution; 5b = in-context merge (reuses People-tab merge UX, not a bespoke swap) + self reachable in People + persistent independent "Who's in" roster in split-up mode. See §8 top for detail.

**Recommended order: Unit 4 → Phase 7c → Unit 5 → Phase 8.**

**PHASE 7c — SETTLED HISTORY + PERSON DETAIL (raised by Phoom 2026-07-15; slotted after Unit 4).** Problem Phoom hit: after "Mark N settled" in the global **Settlement** tab, the person + their logs **vanish entirely** (only a 6s Undo). The faded+struck "stays visible" treatment is already applied on Home / recording-detail / expense-detail, but the global Settlement tab drops a person's card the moment their net hits 0 and the person-sheet nets exclude settled shares — so settled things disappear there with no durable trace or reversal. Fix = settling must feel like "moved to history," not deleted. **Key enabler — settled history is DERIVABLE, no schema change:** `settleShares` stamps every row in one settle action with the SAME `settled_at` ISO timestamp, so `group by settled_at` reconstructs each "settlement event" (who / which shares / when); un-settle = clear `settled_at` for that group (`unsettleShares` already exists). The unused `settlements` ledger stays unused. Two units:
- **7c-1 ✅ DONE + verified live — Settlement → History gets settled events** (GLM). `Settlement.jsx`: `settleEvents` derives settled contribs grouped by **`${settledAt}::${creditor}::${debtor}`** — the person-PAIR is in the key because RecordSettleSheet's "Mark all as paid" stamps several pairs with ONE timestamp and they must stay separate events. New "Settled" section in History (newest first, "X paid you"/"You paid Y", `Money`, relTime, N shares) → tap → `EventSheet` (who/total/full date + underlying shares) → bordered secondary **Un-settle** → `unsettleShares(event.contribs.flatMap(c => c.settleKeys))` (ORIGINAL rows — merge-safe) → person reappears in Balances. Empty state now "Nothing settled or archived yet." QA'd: grouping key + settleKeys correct.
- **7c-2 ✅ DONE + verified live — Person detail screen in People** (GLM). New `src/screens/PersonDetail.jsx` (pushed overlay, mirrors RecordingDetail shell): amount summary (`pairNet` → "X owes you"/"You owe X"/"All settled up"), "In recordings" (recordings the person appears in, tappable → RecordingDetail), "Settled with X" (settled contribs between self+person grouped `${settledAt}::${creditor}::${debtor}`, read-only — un-settle stays in Settlement→History), "Edit" header action → the shared `EditSheet` (now **exported** from `People.jsx`) with FK-guarded remove. App: `onOpenPerson` → `push({type:"personDetail", id})`; People row tap (outside merge mode) → `onOpenPerson` instead of the edit sheet. Merge mode + add-person intact.
- **7c-2b ✅ DONE + verified live — Gate RECORD archiving on fully-settled (Phoom, 2026-07-16)** (opencode). RecordingDetail: `allSettled = logs.length === 0 || logs.every(e => e.status?.kind === "settled")`; Archive button wrapped in `{(allSettled || rec?.archived_at) && ...}`. Reverses the earlier "records archivable while unsettled" asymmetry; consistent with 7c-3b (loose). Does NOT conflict with "settled record doesn't AUTO-archive" (auto vs manual axis).

**PHASE 7c FOLLOW-UPS (Phoom's 2026-07-15 feedback pass on 7c-1). Recommended order: 7c-1a → 7c-1b → 7c-3 → 7c-2 → Unit 5 → audit phase.**
- **7c-1a ✅ DONE + verified live — History sub-nav + search** (GLM). `Settlement.jsx`: `histSeg` (settled|records|expenses, default settled) + `histQ`. History body replaced with a "Search history" input + a lighter text-button sub-nav (thin underline on active, `padIndex` counts) below the main Balances|History pill; only the active section renders. Search filters the active section (settled → either person name; records → recording name; expenses → expense title). Per-section empty lines + "No matches." + original all-empty state preserved. Rows unchanged (EventSheet/detail taps intact).
- **7c-1b ✅ DONE + verified live — Ticking symmetry** (GLM). `Settlement.jsx`: (i) PersonSettleSheet now pre-ticks **nothing** (`useState(() => new Set())`) + a "Check all"/"Clear" toggle → settling is deliberate. (ii) EventSheet share rows are now individually tickable (pre-ticked all, "Check all"/"Clear"); un-settle reverses **only ticked** shares (`unsettleShares(tickedContribs.flatMap(c => c.settleKeys))`), button "Un-settle {N}" disabled when none. Partial un-settle re-derives the event smaller for free (still-settled shares keep `settled_at`); un-settling all removes it. No ledger writes on un-settle.
- **7c-3 ✅ DONE + verified live — Archive loose expenses** (GLM). Migration `alter table expenses add column archived_at timestamptz;` (applied live 2026-07-16). `ExpenseDetail.jsx` Archive/Unarchive header button (loose only); `Home.jsx` filters `!e.archived_at` off the loose feed; `Settlement.jsx` History "Expenses" section now derives `archivedLoose` (`!recording_id && archived_at`, sorted archived_at desc) instead of settled-loose; empty copy "No archived expenses." archived_at is organizational, NOT a balance signal.
- **7c-3b ✅ DONE + verified live — Archive refinements (Phoom feedback on 7c-3).** Three changes: (i) **Gate loose-expense archiving on FULLY SETTLED** — the Archive button (both ExpenseDetail and the new Home inline one) only appears when every ower's shares are settled (or the expense is already archived, so Unarchive is always reachable). Rationale: archiving an unsettled loose expense hides it from Home while its debt still counts in Balances = a source-less debt. Gating on settled makes archive purely organizational. **Deliberate asymmetry:** recordings can still be archived while unsettled (living container); a loose expense is an atom, so hide-with-open-debt is confusing — settled-gate it. (ii) **Inline Archive/Unarchive text button on Home** loose rows, beside "Open detail →", settled-only. (iii) **Remove the "Marked settled/Marked open" Undo snackbar from `ExpenseDetail`'s per-person toggle** — toggle-in-place already IS the undo, so it's redundant. (Global Settlement "Mark N settled" snackbar stays — no in-place toggle there.)

- **7c-4 ✅ DONE + verified live — History open/settled legibility (Phoom, 2026-07-16)** (opencode). `Settlement.jsx` History Records + Expenses rows: per-row `hasOpen = contribs.some(c => (c.recordingId===r.id || c.expenseId===e.id) && !c.settled)`. hasOpen → full opacity, ochre `--open` 6px dot marker (records also get an "open" meta tag), no strikethrough, normal amount colour; fully settled → unchanged faded + `--settled` ✓ + struck. Visual only, no auto-unarchive. **→ PHASE 7c COMPLETE.**

**⚠️ FUTURE PHASE (needs its own design session) — SETTLE AUDIT TRAIL + COMMENTS (Phoom raised 2026-07-15).** Wants: a **comment box on un-settle** (why was this reversed?) + a **durable log of every settle AND un-settle** so suspicious activity can be reviewed; leads on to a **history/comment thread inside each expense log**. **Architectural blocker — this CANNOT be built on the current model:** `settled_at` is a single field (settle = write timestamp, un-settle = clear it), so **un-settling ERASES the settle with no trace**. Phase 7c-1's History is a view of *current settled state*, not an action log — un-settle something and it vanishes from History as if it never happened. An audit trail requires an **append-only event table** (this is finally the job for the currently-unused `settlements` table, or a new `settlement_events` table: action settle|unsettle, timestamp, note, the shares it covered) and the comment needs a column to live in. Deliberately parked — do NOT half-build it into 7c.

**⚠️ PHASE 8-pre — PERFORMANCE + DATA-LAYER REFACTOR (Phoom raised 2026-07-16: "app feels slow/laggy, esp. record ops").** Do this AFTER the v1 feature set is frozen (Unit 5 + whatever audit-trail scope ships in v1) and BEFORE deploy — refactoring against a moving data model means doing it twice. **Diagnosis (grounded in code, not speculation):**
- **Root cause #1 — every mutation re-fetches the WHOLE DB.** `loadSettlementData()` pulls all 6 tables UNFILTERED (expenses/items/members/settlements/recordings/people) + rebuilds ALL contributions, and it's called by `Settlement`, `RecordSettleSheet`, `PersonDetail` on mount AND on every local `bump`. So each transfer tap in RecordSettleSheet = full 6-table reload + full recompute. Scales with total history, not the one record. **This is the main "settle up feels slow."** — **Partly addressed:** P1 killed the per-tap reload (settle taps are now optimistic, no reload). P2 scoped RecordSettleSheet's remaining mount/re-sync load to ONE record via `loadSettlementData(recordingId)` (embedded, indexed query — see below). Settlement/PersonDetail still do the full load on mount (they need the global picture; a shared cache was deferred — invalidation risk).
- ~~**Root cause #2 — sequential settle writes.**~~ **FIXED** — `settleShares`/`unsettleShares` in `balances.js` now batch via `Promise.all` (verified in code 2026-07-17). The #1 reload after them remains.
- **Root cause #3 — reactivity gaps.** "Unarchive a record → must nav to another tab and back to see it update" = stale state; the mutation succeeds but the visible list doesn't re-derive until remount. Wiring issue, not raw speed.
- **Scope (tight — NOT a stylistic rewrite; that's high-risk/low-ROI pre-deploy):** (1) stop full-reloading — load once into a shared data layer / scope queries per record; (2) memoize `buildContributions`; (3) optimistic UI on settle toggles (flip local state instantly, write in background) so taps feel instant; (4) fix reactivity gaps so mutations refresh the visible screen without a remount. Cleanliness is a byproduct of consolidating the data layer, not the goal.
- **Who/model:** 🔴🔴 correctness-must-beat-cost (touches the money IO + cross-screen state) → **runs in Claude Code (me), NOT opencode.** Do it on **Opus 4.8** with the `test-merge`/balances **node tests as a guardrail** (prove the math is unchanged at each step); escalate the session to **Fable 5 xhigh** only if it turns architectural (e.g. a shared data store/context). Not a separate agent — a focused Claude Code session.
- **Immediate quick-win ✅ DONE (verified in code 2026-07-17):** RecordSettleSheet snackbar removed (toggle-in-place IS the undo, same as the ExpenseDetail removal in 7c-3b) + `Promise.all` batching of settle/unsettle writes in `balances.js`. Both landed in the `be6bf25` span. Batching helps but does NOT fix root cause #1.
- **P0 ✅ DONE (2026-07-17, Claude Code) — guardrail tests recreated IN-REPO.** The old 30/30 balances + 19/19 merge node suites lived in a dead session's scratchpad and were LOST. Recreated as **`tests/balances-core.test.mjs`** (plain Node, no framework; **`npm test`** → 40/40). Covers: `toHome` both hops + defaults, `buildContributions` (payer-excluded, native/base/home triple, settleKeys, settled flag, memberless item), alias merge (chain/cycle, redirect, overlap-dedup ÷distinct, self-wash, dedup settled status, original-row settleKeys), `pairNet` (direction/antisymmetry/settled-excluded), `netByPerson` (base key, zero-sum), `directTransfers` (two-payer pairwise, share-backing, no-strand, wash-out), `minimizeTransfers`, `statusForExpense`. **Run `npm test` after ANY change to the money path — it must stay 40/40 through the whole refactor.**
- **Remaining units (run here in Claude Code, one at a time, `npm test` green after each):**
  - **P1 ✅ BUILT (2026-07-17, Claude Code; compiles + boots clean + 46/46 tests; ⚠️ needs Phoom's visual check) — optimistic settle toggles.** Settle taps now flip local state instantly and write in the background; the per-tap full 6-table reload is gone. How: new pure **`patchContribsSettled(contribs, keys, settledAt|null)`** in `balances-core.mjs` (flips any contribution whose `settleKeys` intersect the toggled keys — flows always act on whole contributions, so intersection = toggled; tested to produce EXACTLY the state a rebuild-after-write would). `settleShares(rows, at?)` takes an optional pinned timestamp (optimistic caller and DB store the SAME `settled_at`, so History event grouping stays stable) and both settle IOs now **throw on a failed write**. Failure path everywhere: re-sync via full `load()` + transient notice (new `src/components/SaveError.jsx`) — never silently lie about saved state. Per-screen `chain` ref serializes background writes (no same-row write races). **`RecordSettleSheet`**: toggle + Mark-all optimistic, `bump`/`busy` removed. **`Settlement`**: mutations moved UP to the parent (`commitSettle`/`unsettleEvent`/`undo` patch + background-write + re-sync in one place); `PersonSettleSheet`/`EventSheet` are now dumb (report keys, no writes); `bump` removed (still reloads on `refreshKey`). **Also: dropped the leftover `recordPayment`/`deletePayment` ledger writes in the person-sheet commit + undo** — batch 2 documented ledger writes as stopped, but this one path still wrote them; now the `settlements` table is truly write-free (2 fewer round-trips per commit). **`ExpenseDetail`**: per-person toggle patches `membersByItem` locally + background write (`busy` now only guards delete/archive). PersonDetail untouched (read-only; its mount-load is P2's business). **Known micro-race (accepted):** closing a screen instantly after a toggle can let the underlying screen's refetch land before a slow write; self-heals on the next refresh.
  - **P2 ✅ BUILT (2026-07-17, Claude Code / Opus 4.8; tests 46/46 + build clean; ⚠️ needs Phoom's visual check) — scoped RecordSettleSheet load.** `loadSettlementData(recordingId)` gained an optional record-scope: when passed, it pulls ONLY that record's tree via a single PostgREST-embedded, indexed query (`expenses.select("*, expense_items(*, expense_item_members(*))").eq("recording_id", …)` → flattened back to `{expenses, items, members}`) + full `people` (alias map) + the one recording (so `toHome` still has its base/home rates) + `settlements:[]` (money math never reads it). No-arg call = unchanged full load, so `Settlement`/`PersonDetail` are untouched. `buildContributions` is UNCHANGED (guardrail tests stay 46/46 — they feed the core fixture data directly, not via this IO). `RecordSettleSheet.jsx` now calls `loadSettlementData(recordingId)`; its output filter (`c.recordingId === recordingId`) makes the result identical to before. **Deliberately deferred (not built) with rationale:** (a) the app-wide **module cache** for Settlement/PersonDetail — those need the global picture and a cross-app cache with correct invalidation is a money-bug footgun, low ROI at friend-group scale pre-deploy; (b) **memoizing `buildContributions`** — the bottleneck is network round-trips, not CPU; it already runs once per load. **⚠️ Key visual check (load-bearing):** open a **foreign-currency** record → "Settle up this record" → the transfer amounts AND the faded home-currency lines must match what they were before P2 (proves the scoped load correctly included the recording's exchange rate; if it were dropped, home would wrongly equal native).
  - **P3 ✅ DONE (2026-07-17, Claude Code; build clean + tests 46/46; ⚠️ needs Phoom's visual check) — archive refresh wiring.** Root cause (a swallowed argument, not speed): `RecordingDetail.toggleArchive()` calls `onClose(true)` to signal "changed", but App wired RecordingDetail's `onClose={() => popOverlay(false)}` — a wrapper that DISCARDED the `true` and always popped with `changed=false`, so `refreshKey` never bumped and the underlying Home/Settlement never re-fetched (only a tab-switch, which remounts, showed the update). Every OTHER pushed screen forwards the child's arg (`onClose={popOverlay}`); RecordingDetail was the lone exception, wrapped that way so its plain **back** button wouldn't force a refresh. Fix (2 lines, behavior-preserving for back): App now passes `onClose={popOverlay}` for RecordingDetail (forwards the arg), and RecordingDetail's back button is `onClick={() => onClose(false)}` (explicit no-refresh). Now archive/unarchive → `popOverlay(true)` → refreshKey bumps → Home/Settlement re-fetch immediately; back still no-ops the refresh. (ExpenseDetail's loose-expense archive was already correct — it flips in place via `setDirty(true)`+self-`load()` and signals on back through its direct `onClose={popOverlay}` wiring.) **Visual check:** archive (or unarchive) a record from its detail → on return, Home (or Settlement→History) reflects it WITHOUT a tab-switch.

  - **Detail-screen load jank ✅ DONE (2026-07-17, Claude Code; build clean + tests 46/46; ⚠️ needs Phoom's visual check) — Phoom: "opening record/expense detail, the UI shifts 2–3 times before it settles."** Cause was `RecordingDetail`: it loaded via **6 sequential** round-trips and rendered the header title, "Who's in" roster, and logs **ungated** (only the logs list sat behind `loading`), so the page grew in stages — name pops in, roster block inserts (big downward shove), logs appear. Fix: (1) `load()` now runs the 4 independent queries (profile/recording/members/expenses) in one `Promise.all`, keeping only items→members sequential (6 round-trips → 3 phases), and sets all state together at the end; (2) the whole scroll body + the header Edit/Archive buttons are gated behind `!loading` and wrapped in a light `fadeIn 160ms` — content now appears in ONE pass. `ExpenseDetail` already gated its body (one clean transition); only its header Archive button could flash, so it got a matching `!loading &&` guard. **If any residual shift remains after this, the likely remaining cause is the money-font (Doto) swapping on first open — a one-time, cache-then-stable reflow; preload Doto if it bugs Phoom.** **Visual check:** open a record detail (ideally one with a roster + several logs) → it should appear once, no stair-stepping.

  - **Detail-screen UX pass (2026-07-17, Claude Code; build clean + tests 46/46; ⚠️ needs Phoom's visual check) — three Phoom-requested tweaks.** (1) **Expense archive/unarchive now pops out of context like a record** — `ExpenseDetail.toggleArchive` was flip-in-place (`setDirty(true)`+`load()`); now it awaits the write then `onClose(true)`, so the detail pops and the underlying Home/History refreshes (mirrors `RecordingDetail`). `dirty` still drives the back-button refresh for settle toggles. (2) **Un-settle sheet defaults to UNchecked** — `EventSheet`'s `ticked` init went from `new Set(all)` to `new Set()` (the "Check all"/"Clear" toggle already handled both states), so reopening shares is deliberate. (3) **Un-settle is now RED, not settle-green** — new `--danger: #B23A2E` (muted brick red) token in `index.css`; `EventSheet`'s checkbox fill + the "Un-settle N" button border/text use `--danger` instead of `--settled`, so a reversal no longer reads as a settle. The top "✓ settled" event badge stays `--settled` (it describes the event's state, not the action).

  - **Settle/archive UX pass #2 (2026-07-17, Claude Code; build clean + tests 46/46; ⚠️ needs Phoom's visual check) — two Phoom-requested changes.** (A) **Removed the "Marked settled" undo snackbar** from the global `Settlement` tab (settle had it, un-settle didn't — inconsistent, and Phoom judged it unnecessary). Dropped `snack` state, the `undo()` fn, its auto-dismiss effect, the render, and `setSnack` in `commitSettle`. Un-doing a settle is still fully available via History → Settled → tap event → red **Un-settle** (the deliberate path). (B) **Archive/unarchive now finishes loading in the detail, THEN pops — no more "item lingers then vanishes" flash, and write failures keep you in the detail to retry.** Previously the detail popped immediately (`onClose(true)`), revealing the underlying list still showing the item until ITS async re-fetch landed. New flow: `RecordingDetail`/`ExpenseDetail` `toggleArchive` sets `busy`, awaits the write; **on error → `setSaveErr(true)` + stay in the detail** (retry); **on success → `onArchiveClose()`** (new App prop) instead of popping. `App.archiveClose()` bumps `refreshKey` + `loadPeople` but keeps the detail mounted (showing its busy "…"), sets `pendingPop` ref; a new `handleLoaded` (passed as `onLoaded` to Home + Settlement, called at the end of their `load()`) bumps `loadedTick`; an effect on `loadedTick` pops the stack once `pendingPop` is set — so the detail unmounts only after the underlying screen is fresh. **Self-reload guard (fixes a button-flip):** because the detail stays mounted through the `refreshKey` bump, its own `useEffect(() => load(), [load, refreshKey])` would re-fetch and flip the button label to the opposite state for a frame before the pop (ExpenseDetail `setExp` is the first line of its load, so it flipped fast; RecordingDetail sets state only at the end of its load, so it didn't — but both now guard). Each detail sets an `archiving` ref true right before `onArchiveClose()`, and the reload effect early-returns while it's set, so the detail keeps its pre-press label until it unmounts. **Safety net:** a 1.5s timeout in `archiveClose` pops anyway if no screen reports loaded (covers rarer underlyings like PersonDetail, which isn't wired to `onLoaded`). `RecordingDetail` gained `busy`/`saveErr` state + `SaveError` (it had none); `ExpenseDetail` already had them. While `busy`, the Archive/Unarchive button keeps its label and fades to grey (`--text-4`) as a pressed indication (Phoom's ask — not "…", not a dim/disappear). **Visual check:** archive a settled record/loose-expense from its detail → the Archive button fades to grey briefly, then it pops to an ALREADY-updated list (no flicker of the item). To test the failure path, kill wifi then archive → you stay on the detail with a "Couldn't save" notice and can retry.

  - **REC live-session switch — arm-then-confirm (2026-07-17, Phoom-requested; built by DeepSeek V4 Flash Free in opencode + Claude Code; Phoom-visually-checked). `Home.jsx` only.** Replaced the full-screen **"Switch live session?" modal** (the old `confirm` state + dialog JSX) with **arm-then-confirm on the toggle itself** — same toggle-in-place philosophy as settling; the interaction stays anchored to the card you're acting on instead of yanking you into a dialog. New state = `armedId` (recording id | null). Rules: tap REC on a non-live record **while another is live** → **arm** it (no switch yet); tap the same toggle again → switch + disarm; tap a different card's toggle while armed → the arm **moves** there; **nothing live** → first tap goes live immediately (nothing to confirm); tapping the **live** record's toggle → turns off immediately (reversible, no confirm). Armed visual: knob turns `--accent` but **stays on the LEFT** (Phoom's explicit call — it does NOT move), plus an accent ring on the track; so **grey-left = off · orange-left = armed · orange-right = live**. An armed card slides down a strip (reuses the existing `Accordion`) with a dotted separator, faded accent dot, "Tap the toggle again to switch the live session here." + a `cancel` text button.
    - **An arm is a pending intent, not a mode** — it must not linger, or you'd return later, tap to see what the toggle does, and silently switch your live session. So it **expires after `ARM_TIMEOUT_MS` (5000)** and **any tap elsewhere cancels it** (capture-phase `pointerdown` on `document`, active only while armed). **⚠️ The listener deliberately ignores taps on ANY `[data-rec-toggle]`** — capture-phase pointerdown fires BEFORE React's click, so without that exemption it would disarm first and the confirming second tap would just re-arm (switch appears broken). Don't "simplify" that check away.
    - **Optimistic switch (fixes a grey flash Phoom caught).** `setLive` used to clear the arm instantly then await THREE round-trips (others-inactive → this-active → full `load()`) before `is_active` flipped locally, so the card rendered as neither-armed-nor-live for a few hundred ms. Now it follows the **P1 optimistic pattern**: flip local `feed` state synchronously first (no awaits before it), then write in the **background** with `Promise.all` (the two updates touch disjoint rows), checking each result's `error` (Supabase returns `{error}`, it does not throw) → on failure `setSaveErr(true)` + `await load()` to re-sync, surfacing `SaveError`. **No `load()` on the success path** — the optimistic update already made the UI correct; re-fetching is what made it slow. `setLive`/`onToggle` no longer use `busy` (the `busy` state itself is retained — `doSeed`/"add demo data" still needs it).
    - Also: the small **`Rec` legend label now renders only on the LIVE card** (other cards show a bare toggle) — which is why the armed strip says "Tap **the toggle** again" rather than "tap rec": an armed card has no `rec` label for that wording to point at.
    - **Design-token note (Phoom's call, deliberate):** arming briefly puts `--accent` on two toggles at once (the live one + the armed one), which bends the "one accent element per screen" rule in §6. Accepted — they're the same control expressing one handoff, and it's transient.

  - **P4 — LOAD-JANK + RE-FETCH PASS (2026-07-17, Phoom: "the app still doesn't feel smooth… the window pops up when it hasn't finished loading and the components just pop up one by one"). ALL DONE + Phoom-visually-checked.** The rule adopted: **a screen must never GROW.** Either it has everything at open, or it shows a stable shell (header always visible, so the back button works immediately — never a blank trap) and fills in **exactly one pass** (`!loading` gate + `fadeIn 160ms var(--ease)` + an empty `flex:1` filler while loading so the layout can't jump). This is the same recipe as the earlier detail-screen fix; these screens just never got it.
    - **`ExpenseForm`** (🟠 GLM) — was UNGATED with up to **6 sequential** round-trips, each `setState`-ing as it landed (you watched the network arrive: title, then currency, then recording chip, then people). Only `items→item_members` and `recording→members` are real dependencies; `profiles`/`expenses`/`expense_items` are all keyed on ids already known. Now **2 phases** via `Promise.all` in both add and edit mode, all state set together at the end, body gated.
    - **`CreateRecording`** (🟡 free) — same shape, 3 serial → 2 phases + gate.
    - **`RecordSettleSheet`** (🟡 free) — already gated correctly, but made 3 **independent** calls in sequence (`profiles`/`recordings`/`loadSettlementData` are all keyed on known ids), so the gate lingered ~3× too long. Now ONE `Promise.all` phase. `load` keeps its `[recordingId]` dep — the optimistic `background()` write chain depends on its identity.
    - **TAB SCREENS NO LONGER UNMOUNT (`App.jsx`, 🟠 GLM) — Phoom: "we shouldn't have to re-fetch everything every time, especially for the 3 screens in bottom nav."** Root cause was `{tab === "home" && <Home … />}`: the `&&` **destroyed** the screen on every tab switch (data, scroll position, expanded state) and rebuilt it from zero on return. Now **lazy-mount-then-hide**: `visited` state (`{home:true}` + an effect marking each tab visited) → a tab mounts on FIRST visit and then stays mounted, hidden via an `{position:absolute, inset:0, display: active?"block":"none"}` wrapper (the screens' roots are `height:100%`/`inset:0`, so the wrapper gives them the same box). First visit costs what it always did; every visit after is instant with zero refetch. **⚠️ Load-bearing wiring:** `onLoaded` is now passed **only to the active tab** (`tab === "home" ? handleLoaded : undefined`, same for settlement). Home AND Settlement both call `onLoaded?.()`; with both mounted, a HIDDEN screen's load could otherwise fire the deferred archive-pop early and resurrect the exact "item lingers then vanishes" flash that mechanism exists to prevent. **Known accepted tradeoff:** a mounted-but-hidden Settlement re-runs its full load on every `refreshKey` bump (i.e. every expense save), not just when opened — invisible to the user, and it's what makes returning instant; more queries overall, fine at friend-group scale. If it ever bites, the refinement is to mark hidden screens stale and refetch on re-show (do NOT build that speculatively).
    - **`Settings` now opens with ZERO network + `homeCurrency` lifted into `App`** (Claude Code). Settings re-fetched the `profiles` row AND called `supabase.auth.getUser()` on every open, though App already holds the session and the people roster. Now `App` owns `homeCurrency` (new `loadProfile` useCallback, loaded once, reloaded in `popOverlay(changed)` — Settings is its only writer) and passes `email={session.user?.email}` + `homeCurrency` + `people` down; Settings derives `self` from `people` and seeds local state from the props (with a re-seed effect for the case where Settings is opened before App's first profile load lands). **Note:** other screens still fetch `profiles.home_currency` themselves — folding them onto App's prop is deliberately left to the currency-invariant unit below, which owns home-currency semantics and will touch all of them anyway.
    - **Settle-sheet rows no longer move when you settle someone** (`RecordSettleSheet.jsx:71`) — the sort was `(a.paid - b.paid) || (b.amount - a.amount)`, explicitly pushing paid rows to the bottom, so toggling re-sorted the list under the user's finger. Now `b.amount - a.amount` only. Stable under toggling because a transfer's `amount` sums ALL its contributions **including settled ones** (the IIFE at line ~56 has no settled filter), so the amount doesn't change when you mark someone paid. Settled rows now stay put and just go faded+struck — matching the settled-lifecycle treatment everywhere else.
    - **"Different currency" row in edit-recording** (`CreateRecording.jsx:197`) — the row was a blunt toggle (`if (customCurrency) setCustomCurrency(false)`), so on a foreign record the FIRST tap silently turned it OFF (falling back to home/THB — Phoom: "it suddenly change to thb") and only the second re-opened the picker; a nested tap-target on the chip made it feel arbitrary. Now the row tap **always opens the picker**, and turning it off is an explicit **"Same as home · {homeCurrency}"** first row inside the picker. This was not just confusing — an accidental "off" writes `base_currency: null`, which via the ⚠️ bug below silently rewrites every balance in the record.

**🔴 CURRENCY INVARIANT — C1 ✅ BUILT (2026-07-17, Claude Code/Opus 4.8; migration + backfill APPLIED LIVE by Phoom; build clean; `npm test` 55/55 — the original 46 all still green, proving no existing math moved; ⚠️ needs Phoom's visual check). C2 (home-currency rescale) is the remaining piece — see below.** The bug: changing a record's currency silently rewrote every past balance in it, because the record's currency was **the pivot every expense was converted THROUGH**, read live at display AND settlement time. Phoom's repro: KRW record @0.02, home THB, taxi ₩10,000 = ฿200 → switch the record to USD @33 → ฿330,000.

**The fix (locked architecture — `expenses.home_rate`):** each expense **pins its own native→home rate at creation**; `toHome` reads that pin and never consults the record. The record's `exchange_rate` demotes to what it should always have been: a **pre-fill default for new logs + a display lens**. Changing a record's currency now *cannot* touch an existing debt, because nothing reads through it any more. This is the invariant's rule 2 ([[papaya-currency-invariant]]) implemented literally.

- **Migration — APPLIED LIVE 2026-07-17** (`alter table expenses add column if not exists home_rate numeric;` + a backfill setting `home_rate` = today's derived `toHome(1, …)` per row, so shipping moved no balance). ⚠️ The backfill's `update … from` must join in a **subquery** and match back by id — Postgres won't let the target table be referenced inside `FROM`. Verified live: GBP record → 45.27, KRW records → 0.022/0.026, THB/loose → 1, no nulls. Also added to `schema.sql` for clean-DB applies.
- **`toHome` (`balances-core.mjs`)** — returns `amount * home_rate` when the pin is present, positive and finite. **The live two-hop derivation is KEPT as a deliberate fallback**, not laziness, for: (a) rows predating the column, and (b) callers passing a **synthetic** expense to convert an amount already in a record's base currency (`RecordSettleSheet.jsx:135` does this for transfer totals — there's no stored row to pin). A **0/negative/NaN pin falls back rather than being honoured** — silently zeroing or inverting a debt is worse than deriving.
- **`buildContributions` derives `base` FROM home** — the one deliberate behaviour change, and the piece the original write-up missed: `base` is what an **in-record settlement actually charges people**, and it floated through the record's currency exactly like `home` did, so pinning only `home` would have left settlement amounts corruptible. Now: `base = native` when the expense is already in the record's currency (exact, no float round-trip), else `home / recRate`. Effect: changing a record's currency **re-denominates the whole record at a consistent rate** (₩10,000 → $6.06 — same real value) instead of rewriting debts. Preserves every pre-existing number (tests prove it).
- **`ExpenseForm.save()`** pins `home_rate = expToBase * baseToHome` on **add and edit** (edit re-pins at the current rate — consistent with edit already rebuilding items/wiping settled status). **`seed.js`** pins too (its loose KRW expenses previously had no rate → ₩1=฿1 junk).
- **Tests (`tests/balances-core.test.mjs`, 55/55)** — new "THE CURRENCY INVARIANT" block: Phoom's exact repro asserted before/after (home + native unmoved, `base` re-denominated at the same real value, `baseCurrency` follows the record), **the unpinned row asserted to still produce the 1650× bug** (documents what the pin prevents), corrupt-pin fallback, and **backfill parity** (pinning `toHome(1)` reproduces today's value for every expense shape, and no contribution's home/base/native moves once pinned — this is what made the migration a no-op).
- **⚠️ Still true until C2 lands: do NOT change your HOME currency in Settings** — see below.

**🔴🔴 C2 IS BEING REVERSED — DO NOT COMMIT THE RESCALE (2026-08-05).** It shipped, and Phoom corrupted his live data with it: he entered "1 THB = 33 USD" when the sheet wanted the inverse, and one mistyped number silently multiplied every pinned rate in the account by 33 — every balance wrong, no warning, no undo. The math was correct; the *design* was wrong (asking a user for a raw rate, then bulk-rewriting every rate on their say-so). **Replacement direction = the era model:** changing home currency becomes hard/verified, applies to NEW logs only, and never asks for a rate — because nothing old is converted. The `change_home_currency` RPC and `ChangeCurrencySheet` get **deleted**. C2b (frozen native balances) is KEPT — it's the foundation. See [[papaya-redesign-backlog]] and §10 below. The C2 write-up below is retained only as the record of what's being removed.

**C2 — home-currency rescale ✅ BUILT (2026-07-17, Claude Code/Opus 4.8; build clean; `npm test` 59/59 — 55 prior all green; ⚠️ needs the RPC run live + Phoom's visual check).** Changing home currency in Settings was **already broken and worse than the record bug** — it silently re-read every record's KRW→THB rate as KRW→USD, so *every balance* went wrong at once, no warning, no undo. C1 didn't introduce it, it forced the answer. **The fix (Phoom's locked decision):** picking a new home currency now opens a confirm sheet asking for **one rate** ("1 {oldHome} = ? {new}") and, on confirm, rescales **every `expenses.home_rate` and every `recordings.exchange_rate`** by that factor, then writes the new `home_currency`. Rates are a display lens (the user pins the lens); native amounts and the in-record settlement currency never move — only the home figure re-expresses at the new scale.
- **Atomicity is the whole risk, so it's a Postgres RPC, not a client loop.** New `change_home_currency(new_currency text, factor numeric)` (in `schema.sql`; **⚠️ must be run live before the button works — see the paste block Phoom was handed**). Three writes (expenses, recordings, profile) in ONE function body = one transaction, so a failure can never leave the account half-rescaled. `security invoker` keeps RLS in force; explicit `owner_id = auth.uid()` filters are defense-in-depth; guards reject a null/≤0/NaN factor.
- **Settlement math is untouched** — `buildContributions` is unchanged; C2 only rescales the pinned inputs. `Settlement.jsx` `change_home_currency` RPC replaces the old direct `profiles.update`; the rate-confirm lives in a new `ChangeCurrencySheet` in `Settings.jsx`; failure surfaces `SaveError`. Propagation is free: `onClose(dirty=true)` → App's `popOverlay` bumps `refreshKey` + reloads the profile, so every mounted screen re-derives at the new scale.
- **Tests (59/59)** — new "home change" block mirrors the RPC in pure math: after rescaling by `f`, every debt's **native untouched**, **in-record `base` untouched** (frozen record currency), **`home` = old×f** (same real value), and **loose `base` rescales with home** (loose settles in home, so it's home-denominated by definition — this asymmetry is asserted deliberately, not a bug).
- **⚠️ Until the RPC is run live: still do NOT change home currency in Settings** (the button will error harmlessly — the RPC won't exist — but don't rely on that).

**C2b — frozen-native Settlement balances ✅ BUILT (2026-07-19, Claude Code/Opus 4.8; build clean; `npm test` 63/63; ⚠️ needs Phoom's visual check).** Phoom's follow-up after C2's visual check: the global **Settlement** balances still *changed* when he switched home currency (฿200 → ~$5.80). That's mathematically correct display-lens behaviour, but it violated his "each expense is a constant unchanging snapshot" instinct — so per-person balances now stay **frozen in each debt's own native currency** and never move on a home-currency change. Decision (Phoom, 2026-07-19): show debts per native currency (e.g. "฿200 · ₩5,000"), home only as a faded aggregate.
- **New pure helper `pairNetByCurrency(contribs, aId, bId)`** (`balances-core.mjs`) — direct pairwise net split by each debt's `native` currency, never converted; one entry per non-zero currency, largest first; positive = bId owes aId (mirrors `pairNet`). Re-exported from `balances.js`. Tests: single-currency in native, antisymmetry, cross-currency debts stay separate (not merged), and **unchanged when home currency changes**.
- **`Settlement.jsx` Balances view:** per-person cards now render one line per currency (frozen `Money` in native, coloured by direction) + a faded "~ {home}" convenience total (shown only when >1 currency or the single debt is foreign). Card `rows` carry `byCur` + `homeNet` (homeNet only sorts + feeds the faded total). Person settle-sheet rows **flipped to native-primary + faded home** (were home-primary). **Deliberately left in home** (documented, not a miss): the top **Owed-to-you / You-owe summary** and the sheet footer readouts are cross-person / cross-item aggregates with no single native currency to live in — they must convert, so they still re-express on a home change. History/EventSheet also left home (past settled audit totals; out of the flagged scope).
- **Bonus correctness:** a person whose cross-currency debts happened to home-net to ~0 used to vanish from the list (old filter was on the home net); now both frozen debts show.
- **⚠️ Known inconsistency to resolve next: `PersonDetail.jsx`** (People-tab person screen) still shows a single `pairNet` **home** figure, so it re-expresses on a home change — same complaint, different screen. Not flagged by Phoom yet; fold in the same `pairNetByCurrency` treatment as a quick follow-up.

**C3 — cleanup (optional):** fold the other screens' own `profiles.home_currency` fetches onto App's `homeCurrency` prop (the P4 leftover). 🟢 Easy, mechanical → **free model in opencode**.

**C3 — cleanup (optional, after C2):** fold the other screens' own `profiles.home_currency` fetches onto App's `homeCurrency` prop (the P4 leftover; C1/C2 already own home-currency semantics). 🟢 Easy, mechanical → **free model in opencode**.

Then **Phase 8** punch-list + deploy (Vercel). Deferred to future multi-user phase: claim-to-account (token→real login) + possible-duplicates nudge.

**WORKFLOW (locked with Phoom):** Claude (Claude Code) plans + writes ONE tightly-scoped prompt per unit (always tagged with tier + model + **same-or-new opencode session**) + does code-QA; a model in **opencode** does Easy/Medium/Hard edits (free models / GLM 5.2); **Difficult (Opus/Fable) units run in Claude Code, not opencode.** Phoom runs opencode + does the visual check (Claude can't log into the app — password entry is a refused action). Model tiering + currency invariant: see agent memory ([[papaya-model-tiering]], [[papaya-currency-invariant]]). **After that:** Phase 8 punch-list + deploy (Vercel). Punch-list note: form inputs need `id`/`name` attrs (benign console autofill warning).

**⚠️ OPEN DESIGN QUESTION — multi-currency invariant (Phoom raised 2026-07-15, deferred to its own session).** Before wiring any LIVE exchange-rate API, lock this invariant: **a debt is frozen in the currency it was incurred in; home currency + rates are a DISPLAY lens that never re-computes what anyone owes.** Rules: (1) store debts in native currency, never in home; (2) pin each expense's rate at creation (live API only pre-fills the default, then it's frozen — never convert existing debts through a live/floating rate, or "what you owe" drifts daily); (3) settlement between two people uses ONE shared currency = the record's `base` (NEVER each person's own home currency, which would invent/destroy money via rate+rounding mismatch). Current code already honors #3 (in-record settle uses `.base`, not `home`) and stores `native`/`base`/`home` per contribution. The live-rate work is mainly: pin a per-expense native→base rate at creation + keep native amounts so any home currency derives cleanly. Known display-only gap today: `toHome`'s base→home hop uses the record's single stored `exchange_rate`, which is anchored to whatever home was set when the record was created — so faded home-equivalent lines can drift after changing home currency (balances themselves are unaffected). See [[papaya-currency-invariant]].

Small polish noticed, non-blocking: (a) `ExpenseForm` edit mode's "logged · [ts]" caption shows current time, not original `created_at` — cosmetic; (b) editing an expense deletes+rebuilds its items, wiping settled status on that expense (documented tradeoff).

---

## 9. Build punch-list (fold in as relevant screens are built)

1. **Home** — settings gear opens a stub; wire it to a real **Settings** screen (still ⬜; part of Phase 7 remainder).
2. **Expense form** — "the rest" membership editable (done); delete-item affordance (done). **Batch 3 adaptive pickers — ✅ DONE** (shared `src/lib/suggestions.js`; in-record Everyone=members, loose gets From-last-time/Often chips).
3. **Settlement** — ✅ DONE: global = direct pairwise (`Settlement.jsx`); record scope = direct pairwise item-backed (`RecordSettleSheet.jsx`, toggle-in-place); never routes between strangers. + **Balances | History** sub-tabs.
4. **Recording detail** — ✅ DONE: orange "Settle up this record" primary + add-expense + **Archive/Unarchive**; per-expense settled indicators + faded+struck logs; roster.
5. **Records** — ✅ then **merged into Home** (Phase-6 revision); `Records.jsx` deleted.
6. **Expense detail** — ✅ DONE: `ExpenseDetail.jsx` — amount (+ base equiv if foreign), meta, split breakdown, per-person **toggle** settle (faded+struck), Edit + Delete.
7. **Friends → People** — ✅ roster + segment (All/Accounts/Placeholders) + add/edit/remove built (`People.jsx`). Still ⬜: token **merge/claim** + possible-duplicates nudge.
8. **Settings** — ⬜ not built: home currency (default THB); profile; account/sign-out; token reconciliation entry.
9. **Auth/Onboarding** — move the Create/Sign-in toggle to the email form; keep onboarding minimal. (⬜)
10. **Token wording** — currently "Placeholder" in People. Confirm one user-facing term and use everywhere.
11. **Dual-currency display** (batch 4) + **settle-button reproportion** (batch 5) — see §8.

---

## 10. 🔴 CURRENT PLAN — the 2026-08-05 redesign (START HERE)

**This supersedes "Phase 8 = punch-list + deploy" as the near-term plan.** Phoom returned 2026-08-05 after ~2.5 weeks and scoped a three-part redesign. Full rationale + critique in [[papaya-redesign-backlog]]; the settlement architecture problem is in [[papaya-settlement-model-conflict]].

### State as of 2026-08-05
- **Live data was WIPED + re-seeded** (Phoom ran the wipe; then Home → "add demo data"). Reason: he corrupted every balance by entering "1 THB = 33 USD" (inverted) into C2's rescale; repeated un-multiplying compounded the error to ~413×. Verified clean afterwards — **the Settlement summary total now agrees with the sum of the person cards** (that mismatch was the tell: the summary converts through `home_rate`, the cards show frozen native).
- **C2b is COMMITTED and clean** (frozen-native Settlement balances: `pairNetByCurrency` in the core + the Settlement Balances/person-sheet display). It's the foundation of the new currency model and the thing that survived the data corruption intact. `npm test` **59/59**, build clean.
- **C2 IS GONE from the working tree** (the `change_home_currency` RPC is out of `schema.sql`; `Settings.jsx` writes `profiles.home_currency` and nothing else). ⚠️ **The RPC may still exist LIVE in Supabase — drop it there:** `drop function if exists change_home_currency(text, numeric);`. The rescale is dead permanently: the era model's rule is "never ask the user for a rate."

### The plan (recommended order)
1. **Home currency → era model.**

   **🔨 1a — DATA FOUNDATION ✅ BUILT (2026-08-05, Claude Code/Opus 5; `npm test` 73/73 with the original 59 unchanged; build clean; migration applied live).** C1 pinned each expense's native→home *rate* but not the currency that rate points **at** — so changing home currency left every old pin silently mislabelled. The era fixes that half.

   **🔑 ERAS ARE INHERITED FROM CONTAINERS, never read from the user's setting at the moment of logging (Phoom's decision, 2026-08-05 — this is the load-bearing rule).** A **recording** pins the home currency it was created under and keeps it forever; **every expense filed in it takes that era**, including ones added long after the user switched home currency; a **loose expense** pins the user's home currency at creation. Rationale: a record is a shared context between specific people who settle against each other — the user switching their own home currency is a fact about the *user*, not about that group, so it must never fragment or re-denominate their record. The Korea trip stays a THB-era record after the user moves to USD. **Consequence: a record can never hold two eras, so its single base rate is always era-consistent and `base` derivation is correct as written — mixed-era records are NOT a live concern.** No era table, screen, or section — it's a column on two tables.
   - **New columns `expenses.home_currency` + `recordings.home_currency`** (in `schema.sql`; migration + backfill from `profiles.home_currency` — **APPLIED LIVE 2026-08-05**).
   - **New pure `eraFor(recording, homeCurrency)`** in `balances-core.mjs` (re-exported from `balances.js`) — the inheritance rule, named once and unit-tested, used by both forms.
   - **`buildContributions` carries `homeCurrency: exp.home_currency || <homeCurrency arg>`** — one field, nothing else. `toHome`, `home`/`base`/`native`, `pairNet`, `netByPerson`, `directTransfers`, `pairNetByCurrency` all UNCHANGED. The fallback is why the era moves no number: pre-column rows behave exactly as today, so the app looks identical before and after the migration.
   - **`ExpenseForm`: the rate the user enters is now ALWAYS the expense's own rate to its ERA** (was: to the *record's* base currency, then multiplied by the record's rate — the coupling C1 set out to remove). The card shows whenever expense currency ≠ era, so an ordinary KRW expense in a KRW record finally gets its own rate field instead of silently inheriting the record's. Label reads `Rate to {era}` + `1 KRW = [ ] THB` + a live `₩10,000 ≈ ฿260 in THB` preview — direction is stated three ways, because misreading it is exactly how the live data got corrupted. **When the record's era ≠ the user's current home, a quiet mono line says `this record logs in {era} · your home currency is {home}`** — without it, being asked for a THB rate on a USD home reads as a bug. Pre-fill: the record's own rate when the record's currency matches the expense's → remembered `papaya:rate:{from}:{era}` → 1. `save()` writes that value straight to `home_rate` + `home_currency = era`, and **derives** `exchange_rate` (= 1 when expense currency = record currency, else `home_rate ÷ record's rate to era`) purely to keep `toHome`'s legacy fallback and edit-mode pre-fill alive. Edit mode re-pins at the current rate and pre-fills from `home_rate`, not `exchange_rate`. Also: a **loose expense now defaults to the home currency** instead of hardcoded THB (previously a non-THB user was asked for a rate on every loose log).
   - **`CreateRecording`:** a NEW recording writes `home_currency` = the user's current home. **Editing never touches it** (the update object deliberately omits it). The screen's currency/rate UI now reads the record's **era**, not the current home — otherwise editing a THB-era record on a USD home would both display "USD" and write an `exchange_rate` meaning base→USD into a field the core reads as base→era.
   - **`seed.js`** pins `home_currency` on recordings and expenses.
   **🔨 1b — MAKE ERAS VISIBLE ✅ BUILT + LIVE-VERIFIED (2026-08-05, Claude Code/Opus 5; `npm test` 73/73; build clean; migration IS applied live; Claude drove the UI himself).** The display rule: **every figure derived from a pinned rate is labelled with the era it was pinned to, and amounts from different eras are NEVER added together** (adding them would need a rate between two home currencies — which this model never has and never asks for). Two new pure helpers in `balances-core.mjs`, both re-exported and tested: **`pairNetByEra`** (direct pairwise net of `home` amounts grouped by era; shares an internal `pairNetGrouped` with `pairNetByCurrency`) and **`sumHomeByEra`** (unsigned totals per era, for settled events).
   - **`Settlement.jsx`** — person cards + summary render per era; settled events carry `byEra` instead of one `amount`; the person sheet's per-row faded line uses `c.homeCurrency`; the footer "still owed / settling" readouts are per era. `homeNet` survives **only to order the list** (never shown), since sorting across eras is arbitrary-but-stable, not a displayed claim.
   - **`PersonDetail.jsx`** — the flagged inconsistency is fixed: its summary + settled events are now per era, matching Settlement.
   - **`RecordingDetail` / `ExpenseDetail` / `RecordSettleSheet`** — the faded `DualMoney` home line is labelled with the **record's/expense's era** (`homeCur={era}`), and the `dual` condition keys off the era. Detail screens show a quiet marker when the era ≠ today's home: **`logs in THB · your home currency is USD`** (record) / **`pinned in THB · …`** (expense).
   - **Live-verified with a real home-currency switch (THB→USD→THB on Phoom's own account; nothing was converted or created — C2's rescale is gone, so the switch only writes the setting):** Settlement stayed at **฿573.33 / ฿86.67** frozen in the THB era; the record detail showed the era marker with faded **฿220** lines (pre-1b these would have read "$220"); the expense form on that record asked for **"Rate to THB · 1 KRW = 0.022 THB"** with its note. Restored to THB afterwards.
   - **✅ HOME-PER-ERA IS THE LOCKED DISPLAY MODEL FOR THE GLOBAL TAB (Phoom, 2026-08-05).** This **supersedes C2b's frozen-native person cards**: a person now reads `฿573.33`, one figure per era, not `₩5,000 · ฿200`. It stays invariant-safe because under C1+era each expense carries a **manually pinned** rate to its own era — so the home figure is frozen too, not a floating conversion, and it does not move when the home currency changes (verified live). **The person settle sheet was flipped to match** (home-primary + faded native — the inverse of the record-scoped screens, which still lead with native). `pairNetByCurrency` is now **unused by any screen**; kept + tested in the core because it's the other honest way to show a cross-currency balance, with an in-code note saying so.
   - **Bug caught by actually running it:** `sumHomeByEra` returns `{currency, amount}` while `pairNetByEra` returns `{currency, net}`; the person sheet's footer read `.net` off the wrong one and rendered **"you'd still owe ฿NaN"**. `signedByEra` now maps to net-shaped entries. Verified after: `฿573.33 still owed to you` matches the card, and ticking dinner (`฿333.33`) leaves `฿240`.
   **🔨 1c — HARD/VERIFIED HOME-CURRENCY SWITCH ✅ BUILT + LIVE-VERIFIED (2026-08-05). → UNIT 1 IS COMPLETE.** Picking a currency in `Settings` no longer applies it: it opens a new **`ConfirmCurrencySheet`** (local to `Settings.jsx`) that states both halves plainly — *"New expenses and recordings will be logged in USD"* and *"Nothing you've already logged changes. Records started in THB keep logging in THB, and no rate between THB and USD is ever used"* — plus a mono caveat, *"you can switch back, but logs made in USD stay in USD"*. **It asks for NO exchange rate, by construction** (nothing is converted, so there is no rate to ask for — asking for one and rescaling on the answer is exactly what C2 did).
   - **The guard is a TYPED confirmation** (`Type USD to confirm`, case-insensitive, button disabled until it matches, input border goes accent on match). Rationale in-code: the change is no longer *destructive* (nothing is rewritten) but it IS **effectively irreversible** — once logs exist in the new era, switching back leaves the account permanently mixed. The real failure mode is tapping through without reading, which a plain Confirm button does not stop; typing the code can't be completed without reading it.
   - Failure path: the sheet **stays open** and says `couldn't save — nothing changed. try again.` (`confirmChangeCurrency` throws; the sheet catches). Settings keeps its zero-network open (P4) — the only query is the write itself.
   - Settings' section caption rewritten to the truth: *"The currency new expenses and recordings are logged in. Changing it never touches anything already logged."*
   - **Live-verified:** picking USD opens the sheet without applying; `US` leaves the button disabled; `usd` (lowercase) enables it; Cancel leaves the setting on THB. Deliberately NOT committed on Phoom's account — the write path itself was exercised earlier in 1b's THB→USD→THB check.
   - **Possible refinement (not built, mention only):** the sheet could name the concrete count (*"your 3 recordings stay in THB"*), but that costs a query and a loading state in a confirm sheet; the rule reads fine without it.

   **✅ UNIT 1 COMPLETE (1a data foundation + 1b visible eras + 1c hard switch).** Commit `e203454`. The original open question below was answered in the build: summary + person cards are **home-per-era**. Kept for the record — **⚠️ (ANSWERED) the open question was:** what happens to the "Owed to you / You owe" summary — the only thing left that still needs a rate, and the number that lied to him. Per-currency (`฿1,167 · ₩5,000`) or drop it? **Everything else follows from that answer, including whether the "separate section for old records" he described is needed at all** — C2b already freezes balances per native currency, so old records already keep their currency for free. Likely outcome: home currency collapses to "default currency for new logs" and no era/section concept is needed.
2. **Expense quick wins ✅ DONE — commit `2753005` (2026-08-06, Claude Code/Opus 5; `npm test` 73/73 after every unit; build clean; Claude drove the UI himself in an isolated guest account). 🚀 Pushed + deployed 2026-08-06 — live on Phoom's phone. ⚠️ Still not visually checked by him; two open design notes below.** Four units, all display/interaction only — nothing touches `balances-core.mjs` or reads `home_rate`/`home_currency`/`settled_at`, which is why the money tests never moved.
   - **U1 — "Picked" strip + stable alphabetical order (`PeoplePicker.jsx`).** A pinned `PICKED · 03` row of avatar chips (tap a chip's ✕ to remove) sits above the list. **The list deliberately does NOT re-flow** — picked people keep their place with a ✓ instead of jumping into a group at the top. Rationale in-code: lifting a row out from under a mid-tap finger is the same defect that made settle rows re-sort in P4. Ordering is now **You first, then alphabetical** (`localeCompare`, base sensitivity) via one `byName` comparator applied before the member/non-member split, so the merge-mode rows sort identically. Previously `App.jsx` fetched `people` with no `order` at all, so every picker showed an arbitrary order.
   - **U2 — item pickers scoped to the EXPENSE, not the record (`ExpenseForm.jsx` + a new `memberLabel` prop).** The soft-filter grouping already existed (`memberIds` → "In this recording" / "Everyone else"); the real gap was the **scope** in sliced mode. Item/rest pickers now pass `memberIds={[...whosIn]}` + `memberLabel="In this expense"`, so four people at a dinner lead the list even when eight are on the trip — and the other four sit under "Everyone else", one tap away (never hidden: the "friend joins for one dinner" case still works). **`setTargetAll` ("Everyone") now means Who's-in, not the record's members**, and is no longer gated on `recording` — Who's in always exists in sliced mode, so a **loose** sliced expense gets the chip too (it had none before). Consequence: the suggestion chips moved OFF item pickers and ONTO the Who's-in picker (new `addWhosInPeople`, which adds to `whosIn` **and** the rest, matching `toggleWhosIn`'s add path). Clean division: **assemble the roster in Who's in, pare it down per item.** `addTargetPeople` was deleted (dead).
   - **U3 — chain items (`ExpenseForm.jsx`).** A quiet `+ SAME PEOPLE` pill (new `ChainBtn`, mono/hairline — the one accent on this screen is still Save) on each carve-out **and on "The rest"**, `stopPropagation`-ing so it doesn't open the picker. `chainItem(from)` appends a new item copying **only the member set**; label and amount start empty. Put on the rest too because "dessert for the same group as the main table" is the same gesture as mixers-for-the-whisky-drinkers. Hidden when the source has no members.
   - **U4 — collapsible "Made up of" (`ExpenseDetail.jsx`).** `openItems` Set, **all collapsed by default**: the card shows name · amount · `03 PEOPLE ⌄`; tapping reveals the name chips + the `₩X ea` per-head. **Bonus correctness fix:** the per-head divided by **raw member rows**, while the chips rendered **distinct canonical** people — so after a merge the card disagreed with "Who owes". Both now use the distinct-canonical count, matching `perPerson`.
   - **Verified live** (isolated anonymous account, seeded demo data — see the ⚠️ note below): picker shows `PICKED · 03` + You/Ana/Rui then Marco/Sofia alphabetically; removing Ana → `PICKED · 02`, her ✓ clears, **list order unchanged**; item picker shows `IN THIS EXPENSE` = You/Rui with Ana demoted to `EVERYONE ELSE`; Clear→Everyone restores exactly 2 (Who's-in), not 3 (the record); chain button produces a second item with the same 2 people; expense detail collapsed `03 PEOPLE ⌄` → expanded chips + `₩21,333.33 ea`, which matches the "Who owes you" rows exactly.
   - **Open design notes for Phoom (not bugs):** (a) `PICKED` sits directly beneath the Everyone/Clear chips, so there are now **two stacked pill rows** before the first face — fine at 5 people, worth a look on a bigger roster; (b) `+ SAME PEOPLE` reads slightly louder than `+ Add item` below it.
   - **⚠️ VERIFICATION GOTCHA — the dev browser had a REAL session, not a guest one.** Clicking "Dev: continue as guest" while `localStorage` already holds a session is a **no-op**: `signInAnonymously` doesn't replace an existing session, so the app came up as `2@2.com` showing Phoom's real "Seoul 08 2026 - mango trip" data. **Always check `is_anonymous` on the persisted `sb-*-auth-token` before driving the UI.** To get a clean sandbox: clear the auth-token key from localStorage, reload, then click guest (anonymous sign-in is still enabled in Supabase as of 2026-08-06). Nothing was written to the real account — reads only.
3. **VAT / service charge** (his 2.9) — **🔨 3a (money core) BUILT 2026-08-06 (Claude Code/Opus 5; `npm test` 94/94 with the original 73 unchanged; build clean). 3b (UI) is the next paste. ⚠️ Migration NOT yet run live.**

   **Two decisions locked with Phoom (2026-08-06), both of which shaped the build:** (1) the fee is **a stored field**, not baked into item amounts at save time — so the expense detail can honestly show "฿1,000 + ฿177 service", the fee stays editable, and re-editing can't compound it; (2) entry is **subtotal + percentage** — you type ฿1,000 (what the items add to) and a fee %, the app computes ฿1,177. That matches how a Thai/Korean receipt is actually printed (items, then fees below) and it keeps `total_amount` meaning exactly what "the rest" already auto-fills against.

   **🔑 The insight that made 3a five lines instead of an allocation engine:** allocating a fee *proportionally to each item's subtotal* is arithmetically identical to **scaling every item by one factor**. So there is no per-item fee bookkeeping anywhere — one multiply inside `buildContributions` and the fee flows into `native`/`base`/`home` and every downstream net, transfer, and settle for free.

   **3a — what was built:**
   - **New column `expenses.service_charge`** (numeric, null = no fee), in `schema.sql`. It holds the fee **in the expense's own native currency**, kept OUT of `total_amount` and out of item amounts. **`total_amount` is now explicitly the SUBTOTAL** (commented as such in the schema).
   - **Three pure helpers in `balances-core.mjs`** (re-exported from `balances.js`): `serviceCharge(exp)` (null/0/negative/NaN ⇒ 0), `grandTotal(exp)` (= subtotal + fee — **this is the figure to DISPLAY as "the amount"**; `total_amount` alone understates what the payer's card was charged), and `feeFactor(exp)` (= grand ÷ subtotal, guarded against a zero subtotal so a fee with nothing to spread over charges nobody rather than dividing by zero).
   - **`buildContributions` scales one line:** `nativeAmt = (item.amount) * feeFactor(exp)`. Nothing else in the core moved.
   - **⚠️ THE MIGRATION IS A NO-OP ON REAL DATA, AND THAT IS ASSERTED IN THE TESTS.** Every existing row has `service_charge` null ⇒ `feeFactor` 1 ⇒ every balance is byte-identical. Same safety property as the era model, and the reason 3a is safe to deploy *before* the SQL is run. Migration for Phoom to run: `alter table expenses add column if not exists service_charge numeric;`
   - **Tests (94/94, +21):** the ฿1,000 dinner where ฿700 is shared by three and a ฿300 bottle is for two, with ฿177 of fees. Asserts the fee is **proportional, not per head** (Sofia, who skipped the wine, carries ฿41.30 of fee; Rui carries ฿67.85 — and Sofia is explicitly asserted NOT to land on the ฿292.33 a per-head split would give her), that **every ฿ of the charge is allocated and none invented** (the payer's own share added back sums to exactly ฿177), the **null-charge ⇒ identical-to-no-column** migration proof, all six corrupt-input guards, and that the fee rides through `native`/`base`/`home` so an in-record foreign settle is fee-inclusive too.

   **3b ✅ BUILT + CLAUDE-VERIFIED LIVE (2026-08-06, GLM 5.2 in opencode; code-QA'd + driven in an isolated anonymous account by Claude; `npm test` 94/94, build clean). ⚠️ Not yet visually checked by Phoom; two display gaps below are HIS call.** The migration was already applied live (probed: `select=service_charge` → 200, control fake column → 400).
   - **Code-QA:** all three per-head/item paths in `ExpenseDetail` scaled by `feeFactor`; the whole display sweep uses `grandTotal`; `save()` writes `service_charge: feeAmount > 0 ? … : null`; edit mode derives the % back. **Checked the thing that would have silently broken it: every `from("expenses")` call uses `select("*")`** (incl. `balances.js`'s full AND record-scoped loads), so the new column reaches the money core with no fetch gap.
   - **Live-verified end to end** in a KRW record under a THB era (fee + foreign currency together — the harder path): form caption `₩10,000 + ₩1,770 = ₩11,770 charged`; feed + record row `₩11,770` with faded `฿306`; detail big figure `₩11,770`; items `₩8,239` + `₩3,531` = 11,770; `SERVICE & VAT ₩1,770`; per-head `₩2,746.33 ea` / `₩1,765.5 ea`; "Who owes you" Ana `₩2,746.33`, Rui `₩4,511.83`. Edit reopened with the fee pre-filled `17.7`.
   - **🔑 The check that actually proves it (worth repeating for any money unit):** `ExpenseDetail`'s per-head numbers come from its OWN loop, not from `buildContributions` — so their agreeing with each other proves nothing. Ran the real core against the live rows separately: it returned **the same ₩2,746.33 / ₩4,511.83**, and the **Settlement tab matched too** (Ana ฿1,614.07, Rui ฿1,139.97, Sofia ฿450.67, summary ฿3,204.71 = the sum of the cards — the same diagnostic that exposed the C2 corruption). Proportionality proved numerically: Ana carries **₩413.00** of fee, Rui **₩678.50**, where an equal split would be ₩590 each; every slice incl. the payer's sums to **exactly ₩1,770**.
   - **Two display gaps were found in QA (both MY spec's omission, not GLM's error). (a) FIXED + verified; (b) left, flagged.**
     - **(a) ✅ FIXED (Phoom's call, 2026-08-06):** the form's live per-person peek showed **subtotal** shares (`₩3,333.33` each) while the saved expense charges fee-inclusive (`₩3,923.33`) — the form was previewing a number nobody would ever be charged. Fixed in `ExpenseForm.jsx` for **both** peeks: `perHead` (even-split) and `shares` (sliced). Implementation note: rather than re-deriving the factor, the form feeds a **synthetic expense** `{total_amount: total, service_charge: feeAmount}` to the core's own **`grandTotal`/`feeFactor`** — so the preview cannot drift from what `buildContributions` will charge, and it inherits the same zero/NaN guards. Verified live in a fresh guest account: both modes now read `₩3,923.33` (= 11,770 ÷ 3). `npm test` 94/94, build clean.
     - **⚠️ Consequence worth watching:** `✓ Checks out ₩10,000` (deliberately still the SUBTOTAL — it asserts the carve-outs balance, a different claim) now sits directly above per-person rows summing to `₩11,770`. Semantically correct but visually unexplained; if it reads oddly the cheap fix is a label on the peek, not a change to either number.
     - **(b) NOT fixed, low severity:** the **rate card preview** reads `₩10,000 ≈ ฿260 in THB` off the subtotal, understating the expense's era value (`₩11,770 ≈ ฿306`). Left because that card exists to sanity-check the **rate**, not the amount.

   **3b — the UI half as specced (🟠 GLM 5.2):** a fee row in `ExpenseForm` (one **percentage** input, amount computed — deliberately NOT two linked inputs, which is a classic cursor-jump/loop bug source), `save()` writing `service_charge`, edit-mode deriving the % back from the stored amount, and a **display sweep** so every screen shows `grandTotal` instead of `total_amount`. **⚠️ The correctness-critical part of 3b is `ExpenseDetail.jsx:90` and `:213`** — those compute per-head amounts from `it.amount` **directly**, not from `buildContributions`, so they must be scaled by `feeFactor` or the detail card will disagree with "Who owes" and with Settlement. That is exactly the class of bug unit 2's U4 already had to fix once.
   **3c — Phoom's feedback pass on 3b + unit 2 ✅ DONE + Claude-verified live (2026-08-06). Phoom's verdict on the rest: "3b everything works as expected", "unit 2 everything works as expected", "p1/p2 worked".** Four changes, all display/interaction:
   - **`✓ Checks out` now shows the CHARGED total** (`₩11,770`), not the subtotal. This resolves the tension flagged in 3b's QA — the row now agrees with the per-person peek beneath it and with what the expense reads as everywhere else. The `balanced` validation still runs on the **subtotal** (items must add up to it); that's what the *label* asserts, and only the figure changed.
   - **Expense-detail item rows state what they're made of:** `₩11,770` with a quiet mono `10,000 + 1,770` beneath (Phoom's ask: "not just show total but show both total and amount before sum ex. 824(700+124)"). Without it you can't tell why an item reads 824 when 700 was ordered. Only rendered when there IS a fee.
   - **`+ SAME PEOPLE` → a duplicate ICON** (28×28, two-sheets-with-a-plus glyph, `--text-3` on a hairline square). Fixes 3b-QA's note (b): the text pill read louder than the `+ Add item` button below it. `ChainBtn` keeps its name, props and behaviour — only its rendering changed.
   - **🔴 UNSAVED-WORK GUARD on the expense form** (Phoom: "press back button just ditch everything… it should at least confirm"). Back now opens a confirm ("Discard this expense?" / edit mode: "Discard changes?") with **Keep editing** / **Discard** (`--danger`). **Dirtiness is measured against a snapshot taken once loading finishes**, not a "user typed something" flag — that's what makes it correct in EDIT mode, where every field starts pre-filled and a touched-flag would always say yes. A clean form still exits silently (verified).
   - **Verified live in an isolated guest account:** `✓ Checks out ₩11,770` with peek rows 3,923.33 × 3 = 11,770; detail `The rest ₩11,770 / 10,000 + 1,770`; icon renders 28×28 with no text; dirty form → confirm → Keep editing returns, Discard exits; clean form → exits with no prompt. `npm test` 94/94, build clean.
   - **DEV guest button KEPT and committed** (Phoom left it to Claude's judgement). It's `import.meta.env.DEV`-gated so it cannot reach a production build, and it is the only way Claude can verify UI work without touching real data. Keep the [[papaya-guest-verification]] gotcha in mind: it's a no-op if a session already exists.

4. **🆕 ANDROID HARDWARE BACK ✅ BUILT + CLAUDE-VERIFIED IN-BROWSER — commit `d48f49b`, PUSHED + DEPLOYED 2026-08-06 (Claude Code/Opus 5; build clean; `npm test` 94/94 untouched — this unit goes nowhere near the money core). ⚠️ NEEDS PHOOM'S REAL-DEVICE CHECK — Claude cannot press an Android back button.** Phoom, from his S24 Ultra: "pressing back leads me to home, not hide the keyboard or go back to the last screen." Cause: navigation was pure React state (`tab` + the overlay `stack` + every screen's own sheets) with **no browser history entries**, so Android's back acted on the *browser* and left the PWA — mid-expense.
   - **New `src/lib/backstack.jsx`.** Anything dismissible registers a **layer** while it's open via `useBackLayer(open, onBack, level?)`; one back press closes exactly one layer, the topmost. `BackStackProvider` wraps `<App/>` in `main.jsx`.
   - **🔑 History is kept in step by RECONCILING DEPTH, not by pairing each push with a matching pop.** Every entry we create records how many layers were open at the time (`history.state.papayaDepth`), and an effect pushes or rewinds entries until the browser agrees with us. This is what makes it robust: a layer is allowed to **refuse to close** (the expense form's discard guard opens a confirm instead), and the mismatch self-heals on the next render instead of corrupting the stack. Verified live: back on a dirty form popped an entry, the form declined, the confirm opened, and depth re-pushed to 2 — no drift. `history.length` stayed flat at 3 across a full 20-step run, so entries don't accumulate. Guard: the rewind is only ever called with a **strictly negative** delta — `history.go(0)` reloads the page.
   - **`onBack` is the screen's OWN back handler, not a second path.** ExpenseForm's layer calls `goBack`, so hardware back hits 3c's discard confirm — one guard, two triggers.
   - **🔴 THE ORDERING BUG WORTH REMEMBERING (found by actually driving it, not by reading):** layer order can NOT be registration order. **React runs child effects before parent effects**, so re-showing a kept-mounted tab (P4's lazy-mount-and-stay) re-registered People's open sheet BEFORE App's tab layer — and one back press closed the sheet *and* left the tab, jumping straight to Home. Fix = an explicit **`BACK_LEVEL`** (`TAB: 0` < `SCREEN: 10` (default) < `SHEET: 20`), topmost = highest level then most-recently-opened. Only the tab layer needs a non-default level today; within one screen, tap order is already correct.
   - **Tab back = Home, then exit** (Phoom's call, the Android convention) — replaying full tab history gets confusing fast.
   - **Kept-mounted tab screens gate their layers on a new `active` prop** (`People`, `Settlement`) — a hidden screen's open sheet must not swallow a back press taken on Home. Verified: sheet open in People → switch to Home → back does nothing → return to People → the sheet is still there and back closes it.
   - **The soft keyboard is deliberately NOT a layer** — Android dismisses it before the page sees a back event. It only felt broken because the whole app was exiting. (The app's own **keypad** IS a layer, so back dismisses it before leaving the form.) Home's `armedId` is also not a layer: it self-expires and back would look like it did nothing.
   - **~20 layers wired:** the 6 overlay screens (each owning its back semantics), ExpenseForm's keypad/pickers/currency/discard-confirm, CreateRecording's picker+currency, ExpenseDetail's delete confirm, RecordSettleSheet's mark-all confirm, Settings' currency/typed-confirm/edit-self, PersonDetail's edit+unmerge, People's add/merge-mode/merge-confirm, EditSheet's remove confirm, PeoplePicker's merge-mode+confirm, Settlement's person + event sheets, App's tab + stub. **Convention: a sheet's own open/close belongs to the PARENT that owns the state rendering it; the sheet registers only what's dismissible INSIDE it** (avoids double-registration).
   - **Verified in-browser** (isolated anonymous guest, seeded): tab→Home; form clean (keypad→close); form dirty (keypad→confirm→keep editing→confirm→Discard); nested rec-detail→add-expense unwinding one level per press; picker closed by its in-app Done button reconciling with no stray entry; two stacked overlays unwinding one at a time.
   - **⚠️ NOT verifiable here, for Phoom's device pass:** (a) **back at depth 0 actually exiting the app** — in this harness we sit on the first history entry so `back()` is a no-op; on a PWA the launch entry is first, so it should exit; (b) real soft-keyboard dismissal; (c) Settings' three inner sheets are wired with the identical one-line pattern proven elsewhere but weren't individually driven (the selector wouldn't open the currency row).

4b. **🆕 SAFE-AREA + PERSON-RENAME REFRESH ✅ DONE + PHOOM-VERIFIED ON HIS S24 — commits `cc2c178` + `fb5b509`, PUSHED + DEPLOYED 2026-08-06 (Claude Code/Opus 5; build clean; money core untouched, tests stay 94/94).** Took **two passes** — the first fix was real but insufficient; read both bullets, the second one is the actual cause. **Accepted tradeoff Phoom signed off on: pull-to-refresh no longer works** (`overscroll-behavior: none`) — it was the gesture most likely to trigger the bug, and closing + reopening the PWA still reloads, so nothing is lost. Two unrelated bugs Phoom hit on his S24 after unit 4, fixed together as one small deploy.
   - **🔴 BOTTOM NAV RENDERED BEHIND SAMSUNG'S SYSTEM NAV BAR** (Phoom: "on refresh the bottom nav hides under the samsung native home bar and becomes scrollable"). Confirmed from a frame of his screen recording: in the installed PWA the tab bar is simply *not there*, just its top hairline above the black system bar, and the page gains phantom scroll. **Cause: `viewport-fit=cover` in `index.html` with ZERO `env(safe-area-inset-*)` anywhere in the codebase** (grepped — not one use). `cover` tells the browser to extend the layout viewport UNDER the system bars/notch; nothing then compensated, so `100dvh` included the ~48dp nav-bar strip and the 64px `BottomNav` landed inside it. **Fix = delete `viewport-fit=cover`.** The default (`contain`) insets the viewport to the safe area automatically, which fixes the nav **and all ~16 other bottom-anchored elements at once** — every bottom sheet (`position:absolute; bottom:0`) in ExpenseForm/CreateRecording/Settings/People/PersonDetail/Settlement/RecordSettleSheet/PeoplePicker/App's StubSheet, plus ExpenseForm's keypad bar — none of which had safe-area padding either. Phoom hadn't hit those yet only because they're taller and stayed mostly tappable.
     - **⚠️ THAT ALONE DID NOT FIX IT — second pass, same deploy day.** Phoom re-tested on the new build (proven live: the rename fix below worked) and reported the nav is fine at first but **goes back under the bar after a refresh**. So `cover` was a real bug but not THE bug; the "works, then breaks on reload" signature points at **`100dvh` being measured once against a viewport that still includes the system nav-bar strip, and never re-settling** (no resize event fires, so it can't self-correct). Fix = **stop asking for a viewport height at all**: `html, body { overflow: hidden; overscroll-behavior: none }` (the document must never scroll — and that also kills pull-to-refresh, the gesture most likely to leave the viewport in that state) and `#root` becomes `position: fixed; inset: 0` (sized against the viewport directly). Over-constrained left+right+`max-width:430px` with `margin:auto` still centers it. All four `100dvh`/`100vh` uses are gone from the codebase (App's shell + Splash + auth screen → `height:100%`); the auth screen got its own `overflowY:auto` since the document can no longer scroll for it. **Verified in-browser at 375×812:** `#root` is exactly 0→812, `documentElement.scrollHeight === innerHeight` (page cannot scroll), bottom nav sits on the bottom edge, the expense form's keypad bar anchors correctly, and inner scroll regions still work.
     - **Why not add `env()` padding instead:** `cover` buys edge-to-edge artwork behind the status bar, and this is a flat cream list app with none — so it was pure cost. Adding insets would have meant touching ~16 inline styles (and `position:absolute; bottom:0` resolves against the *padding box*, so a single `#root` padding does NOT fix the sheets — a real footgun if anyone tries the "one-line" version of that approach). A comment above the meta tag records this: **only reintroduce `cover` together with safe-area padding on every bottom-anchored element.**
   - **Renaming a person didn't show until an app refresh** — `PersonDetail.rename()` wrote to `people` then called its own `load()`, but `load()` fetches **settlement data**; the name/emoji on screen come from the **`people` PROP**, which only `App` owns. Fix: call `onChanged?.()` (already wired by App to `loadPeople()` + a `refreshKey` bump, which re-runs `load()` for us) instead of the local `load()`. `unmerge()` two lines below was already doing exactly this — `rename` was the lone omission. **Verified in-browser** (isolated anonymous guest): People → self → Edit → rename → the detail header, the roster row underneath, and the "SETTLED WITH …" legend all update instantly, no reload. `Settings.saveSelf` was checked and is **not** affected — it holds `self` in local state and updates it optimistically, plus `onClose(dirty)`.

5. **🔜 NEXT — Settle-up minimization** (his item 3) **+ the parked audit trail, together.** His arithmetic is right (note: multiple valid minimal answers exist). **This reverses a deliberate Phase-6 decision and re-opens a real conflict — read [[papaya-settlement-model-conflict]] before planning it.** Three separate asks (minimized transfers, audit trail, payment evidence) all converge on the unused `settlements` ledger table, which already has a `note` column. Its own design session, ideally a FRESH one. 🔴/🔴🔴.
   - **Phoom's scoping decisions (2026-08-06) — locked:** (i) **minimization lives ONLY in the record settle sheet, never in the global Settlement tab** (his reason: a record's party know each other, the global tab spans the whole roster — note the global tab already never minimizes, so this confirms existing behaviour rather than changing it); (ii) it is a **TOGGLE, not a replacement** — default stays today's item-backed "who owes who", a button switches to a minimized "fewest payments" view.
   - **⚠️ The scoping does NOT dissolve the conflict.** Phoom reached (i) via the stranger-routing argument, but that is not why `minimizeTransfers` was pulled from the record — it was pulled because a minimized transfer **can't be marked paid** without leaving a residual and a stuck state (a bug hit in live testing). That defect is about *tapping*, not about who knows whom, so it survives into the toggle.
   - **The one question the toggle forces: what happens when you tap a row in the minimized view?** The cheap answer (rows are a checklist; settle everything once all are ticked) breaks on real behaviour — people settle over days and tick-state has nowhere durable to live; persisting ticks IS a ledger minus the honesty. **Recommendation unchanged: build the payment ledger** (balance = shares − payments). Then the toggle is trivially correct — both views group the same question, and recording a payment is truthful whether or not the rest of the plan has happened — and it's the same foundation the audit trail and payment-evidence photo need.

   ### 🔴 SUPERSEDED — the free-form payment ledger below was REPLACED the same day. Read the SUMMARY MODEL section that follows it.

   Commit `fb1ce6f` locked a payment-ledger design (balance = shares − payments). Hours later Phoom proposed something better and it was adopted; the ledger text is kept only because its *reasoning* still explains why a plain view-toggle can't work. **Build the summary model, not this.**

   ### ~~✅ PHOOM DECIDED 2026-08-07 — BUILD THE LEDGER, LOG EVERYTHING IN ONE TABLE~~ (superseded)

   Both recommendations accepted: (1) **build the payment ledger** (not the cheap read-only plan, not defer); (2) **one append-only table logs payments AND every share settle/unsettle** — so un-settling stops erasing itself, which was the original July audit-trail complaint. Three sub-units below; **do NOT collapse them into one drop.**

   **The concrete case that justifies all of it (Phoom's own example, use it in tests):** A owes B 200, B owes C 300, C owes A 500. Nets are A +300, B −100, C −200 → the efficient plan is **B pays A 100, C pays A 200**. Today's direct view shows **"A pays B 200"** — the *opposite direction* — so there is no share to tick for "B pays A 100". And once the whole plan completes, every net is zero while **every original share is still open**: the plan discharges *balances*, not *items*. That gap is not closable by any allocation rule; it needs payments to be their own fact.

   **Load-bearing safety property (same as the era + service-charge migrations): the migration is a PROVABLE NO-OP on live data.** No payment rows exist yet ⇒ every balance is byte-identical ⇒ the code is safe to deploy before the SQL runs, and **the existing 94 tests staying green IS the proof** — do not rewrite them to accommodate payments.

   #### 5a — ledger + money core. 🔴🔴 Claude Code (Evie/Opus), **fresh session**, tests-first.
   - **New table `settlement_events`** (in `schema.sql`; migration for Phoom to run). Columns: `id`, `owner_id`, `kind` (`'payment' | 'settle_shares' | 'unsettle_shares'`), `from_person`/`to_person` (payer/payee for a payment), `amount` + `currency`, **`home_rate` + `home_currency`** (pinned at creation, exactly like `expenses` — a payment obeys the currency invariant too, see [[papaya-currency-invariant]]), `recording_id`, `shares` jsonb (`[{itemId, personId}]` for the share-event kinds), `note`, **`voided_at`** (undo = write a void, NEVER delete — that's what makes it append-only), `created_at`. Index on `owner_id` + `recording_id`. RLS = the same plain `owner_id = auth.uid()` check as every other table — **never a cross-table policy lookup** (the v1 recursive-RLS bug).
   - **Only `kind='payment'` with `voided_at IS NULL` affects balances.** The other kinds are pure log. This is the whole discipline of the table; state it in the schema comment.
   - **DROP the old `settlements` table** and delete the dead `recordPayment`/`deletePayment` from `balances.js` (nothing has written that table since Phase-6 batch 2; P1 removed the last two writers). ⚠️ **Have Phoom confirm `select count(*) from settlements;` is 0 before dropping** — it's production DDL even on a table we believe is empty.
   - **🔑 Core design that avoids signature churn across every screen: represent a payment as a SYNTHETIC CONTRIBUTION.** A payment of N from X to Y is arithmetically a contribution with `debtor: Y, creditor: X` (it cancels X's debt to Y), `settled: false`, `settleKeys: []`, plus a `paymentId` marker. Then `pairNet`, `pairNetGrouped` (→ `pairNetByCurrency`/`pairNetByEra`), `netByPerson` and `directTransfers` all work **unchanged**, because they only ever sum signed amounts over debtor/creditor pairs. `buildContributions` appends these when `data.payments` is present (`loadSettlementData` always supplies the key, so it's automatic everywhere and no screen can forget to include them). Screens that must exclude payments filter `!c.paymentId` — a positive, greppable marker rather than an omission.
   - **`loadSettlementData`** gains `payments` in both the global and the record-scoped path (the scoped path currently hardcodes `settlements: []`).
   - **⚠️ Known spot 5b must handle, flag it don't fix it here:** `RecordSettleSheet`'s local pair math computes `paid = p.settled === p.total`; payment atoms are never `settled`, so a pair fully cleared by the ledger would never show as paid. That's 5b's design problem.
   - **Unaffected by design (verify, don't touch):** `statusForExpense` reads raw member rows; `ExpenseDetail`'s per-person rows build from items directly. Per-item settled lifecycle (faded+struck on Home, record detail, expense detail, History) is **not degraded** — `settled_at` stays exactly as it is.
   - **Tests to add** (on top of the 94, which must stay green untouched): Phoom's 3-way cycle nets to zero after the two payments; a payment does not flip any share's `settled` flag and vice-versa (no implicit double-count); a **voided** payment contributes nothing; a payment pins its own era and does not move when home currency changes; an **overpayment** correctly flips the net direction; and a payment with no matching shares still nets (the cycle case — the whole point).

   #### 5b — minimized toggle in the record settle sheet. 🟠 GLM 5.2, opencode.
   Toggle "who owes who" (default, unchanged) | "fewest payments". Minimized rows tap → record a payment in the record's base currency (optimistic + background write, the P1 pattern); tap again → void it. Payments must also be **visible as their own rows** in the record so they are never invisible balance-movers. Answer the `paid` question flagged above.

   #### ~~5c — audit-trail surfaces~~ (superseded — see 5d below)
   Settlement → History gains an events/payments section; notes capturable on void ("why was this reversed?"); voided rows shown struck. This is where the July ask finally lands.

   ---

   ### 🔴 THE SUMMARY MODEL — LOCKED 2026-08-07 (Phoom's design). THIS IS WHAT TO BUILD.

   **Phoom's move: minimization is not a VIEW, it's an ACTION.** You invoke it on a record (with verification); every currently-open debt is **frozen** and replaced by a small set of **transfers**; you settle the transfers instead. Reversible only while nothing in it is settled.

   **Why this is right, and why the toggle was wrong.** A view is a function of current state — it recomputes whenever anything changes, so you can never point at it and say "that's the plan we agreed." Worse, the old bug wasn't the *routing*, it was that the underlying shares kept living independently, so "B paid A ฿100" and "A owes B ฿200" were both true at once and neither knew about the other. **Freezing the shares kills that ambiguity at the source** instead of papering over it with an allocation rule. In accounting terms this is a **closing entry**: take everything open, net it, post the result, mark the originals closed. **This also means free-form payments are NOT needed** — the 3-way cycle is solved by consolidation, so the payment-ledger design above is genuinely superseded, not merely deferred.

   **🔑 THE ONE RULE that makes "repeatable" fall out for free:**
   > **Open debt = anything not settled AND not superseded.**

   It applies identically to shares and to transfers, so a second summary sweeps up new expenses *and* any unpaid transfers from the first with zero special-casing — a transfer is just another thing a later summary can close.

   **Naming (Phoom's call):** the object is a **"Summary"**, its rows are **"transfers"**. ⚠️ Evie flagged that "summary" reads like a view; the resolution is that **the copy carries the weight** — the button is a VERB (*"Create summary"*), closed expenses read *"in Summary 1"*. Do not soften those into nouns. **Never call these "tokens"** — that word already means a placeholder person in Papaya.

   #### Data model — 2 tables + 1 column
   - **`settlement_events`** — the append-only log. A summary IS an event (`kind='summary'`), so it lives where it belongs; reverting **appends** `kind='summary_reverted'` rather than erasing. Also carries `settle_shares`/`unsettle_shares` (the July audit ask) and `note`. Columns: `id`, `owner_id`, `kind`, `recording_id`, `shares` jsonb, `note`, `created_at`.
   - **`summary_transfers`** — the live obligations. `id`, `owner_id`, `summary_id` → `settlement_events(id)`, `from_person`, `to_person`, `amount` + `currency`, **`home_amount` + `home_currency`** (see the wrinkle below — pin the AMOUNT, not just a rate), `settled_at` (behaves exactly like a share's — same lifecycle, same faded+struck treatment), `superseded_by` → a later summary's id.
   - **`expense_item_members.summary_id`** — the entire freeze mechanism. Set ⇒ the share is closed and excluded from balances. Null it ⇒ back to normal.
   - **DROP the old `settlements` table** + the dead `recordPayment`/`deletePayment` in `balances.js`. ✅ **Phoom confirmed `select count(*) from settlements;` = 0 on 2026-08-07** — safe to drop.
   - RLS: the same plain `owner_id = auth.uid()` check as every other table. **Never a cross-table policy lookup** (the v1 recursive-RLS bug).

   #### Revert rule (precise)
   Only the **latest** summary can be reverted, and only while **none of its transfers are settled**. You cannot unwind Summary 1 while Summary 2 sits on top of it. Revert = null the `summary_id` on everything it froze + append a `summary_reverted` event. Settling even one transfer locks it until you un-settle that transfer first (Phoom's explicit design — the guard, not a trap).

   #### Money core (5a)
   - `buildContributions` **skips shares with `summary_id` set** — they're closed, not settled; a distinct state.
   - New `buildTransferAtoms(data)` turns unsettled, non-superseded transfers into **synthetic contributions** (`debtor`/`creditor` pair, `settled: false`, `settleKeys: []`, a `transferId` marker) — the same trick the ledger design used, so `pairNet`, `pairNetGrouped`, `netByPerson`, `directTransfers` all work **unchanged**. Appended by `buildContributions` when `data.transfers` is present; screens that must exclude them filter `!c.transferId`.
   - New pure **`planSummary(contribs, scope, key='base')`** → `{ transfers, shareKeys }`: wraps `minimizeTransfers` over `netByPerson`, and returns the share keys to freeze. This is the function the preview-confirm renders BEFORE anything is written.
   - `loadSettlementData` gains `transfers` in **both** the global and record-scoped paths.
   - **Unaffected by design (verify, don't touch):** `statusForExpense` reads raw member rows; `ExpenseDetail` builds per-person rows from items directly. `settled_at` semantics are completely unchanged.

   #### ⚠️ The home-currency wrinkle — a real 5a decision
   In a foreign-currency record every expense pins its **own** native→home rate, so netting five expenses into two transfers has **no unique correct home split** (it's a transportation problem, not a division). **Decision: pin each transfer's `home_amount` explicitly**, converting the whole summary at the **record's own rate** (`recordings.exchange_rate`) — one explainable number, and pinning the amount rather than re-deriving it keeps the invariant ([[papaya-currency-invariant]]) intact. **Acceptance criterion: the record's home TOTAL must be identical before and after a summary**; individual people's home figures may shift by a hair, and that is accepted.

   #### 🔑 Net-preservation properties — these are the TESTS, not just prose
   - **A person's net NEVER changes across a summary.** This is the guarantee minimization exists to keep. Assert it for every person.
   - **Within the record, gross can only shrink** — after a summary you are purely a creditor or purely a debtor, because the plan discharges nets. (You owe B ฿100, C owes you ฿100 ⇒ the plan is "C pays B ฿100" and you vanish from it.)
   - **⚠️ Globally, gross can occasionally GROW — accepted, and Phoom was told.** Summarising one record can break a cancellation you had with someone across two records: you owe Rui ฿50 in Korea, Rui owes you ฿50 from a dinner (today they cancel to ฿0/฿0); summarise Korea, the plan pairs you with Sofia, and now Rui owes you ฿50 while you owe Sofia ฿50. **Net still zero.** Write this as a test that documents the chosen behaviour (same spirit as the "unpinned row still produces the 1650× bug" test).
   - Minimization can also create a transfer between You and someone you **never shared an item with**. Accepted — Phoom's criterion is that a record's party know each other.

   #### Sub-units — four, and do NOT collapse them
   | | | |
   |---|---|---|
   | **5a** | data model + money core + `planSummary` | ✅ **BUILT 2026-08-07** (Claude Code/Opus 5) — see below |
   | **5b** | the summary flow in the record: preview-confirm, transfers list, revert | ✅ **DONE 2026-08-07** (GLM 5.2, Phoom-verified) — see below |
   | **5c** | the **"closed"** treatment across four screens | 🟠 GLM 5.2 — **NEXT**, and Phoom asked for it directly |
   | **5d** | audit surfaces in Settlement → History | 🟠 GLM 5.2 |

   **5b** — "Create summary" opens a **preview-confirm showing the actual plan** (*"5 expenses · 12 shares will be closed and replaced by 2 transfers"*). **Deliberately NOT a typed confirm** — that was right for the home-currency switch because it's permanent; this is reversible while unsettled, so typing would be theatre. Transfers settle optimistically (the P1 pattern). Revert per the rule above.

   **✅ 5b DONE 2026-08-07 (GLM 5.2 in opencode; Claude code-QA'd + drove it in a guest account; Phoom verified on device — "work accordingly", all steps but Android back).** One file, `RecordSettleSheet.jsx`. `loadSummaries` joined the existing parallel load; summary transfers excluded from the direct list (renamed `directRows`) and drawn as their own rows; a Summary block per summary (skipped entirely when all its transfers are superseded); optimistic settle via `settleTransfer`/`unsettleTransfer` with settled rows staying faded+struck; preview-confirm showing the real plan; Revert on the newest summary only, gated on `canRevertSummary`; back layers for both new dialogs. **GLM caught a real one unprompted:** an optimistically-settled transfer is still an *unsettled atom* inside `contribs` (those only rebuild on `load()`), so the plan would have re-netted money just paid — `planScope` now excludes transfers whose live `summaries` row has a `settled_at`.

   **🔴 5b FOLLOW-UP FIX (Claude, same day) — Phoom: "I tried create summary 5 times, nothing changed, but when I revert I have to do it 5 times. Very confusing, thought the app broke."** The gate was `plan.transfers.length > 0`, which stays true forever: re-netting an already-minimal set reproduces it, so every press stacked another summary that superseded the last — and since **only the latest summary can be reverted**, five presses cost five reverts. Fix is one condition: **`plan.shareKeys.length > 0`**. `shareKeys` is empty exactly when no item-share is still open in scope, i.e. nothing has happened since the last summary — the honest test for "this would do nothing." Verified live in a guest account: button present → create → **button gone** (survives a reload) → insert a new expense → **button back**, with the new debt in "Who pays who" beneath the Summary block.
   - **Phoom also asked to disable it when the record is "already the least transactions possible" (e.g. single-payer).** Deliberately NOT done, and he was told why: once 5c lands, a summary also **locks the record's expenses**, which is a real effect even when the payment count doesn't drop. Disabling it now and re-enabling it one unit later is churn, and it would leave a simple record with no way to lock. The quiet "this won't reduce the number of payments" line stays; **5c should rewrite that copy to say what it DOES do.**
   - Cosmetic, pre-existing (not from 5b): transfer rows read "You pays Rui" — the `{from} pays {to}` template predates this unit and shows the same on direct rows.

   **~~5c/5d as originally planned are SUPERSEDED~~ — see §10.5R below.** Phoom used 5b on a real, still-growing record and the sheet didn't hold up. The freeze-on-create model and the bottom-sheet UI are both replaced. 5a survives.

   #### Accepted limitations (do not solve)
   - **Transfers settle all-or-nothing** — "B owes ฿100, paid ฿60" is not representable, same as today's rows. Partial payment would want a real payment ledger underneath, which this model avoids needing.

---

## 10.5R 🔴 THE SUMMARY-GROUP REDESIGN — LOCKED 2026-08-08 (Phoom). BUILD THIS.

**This supersedes the 5b sheet UI and the original 5c/5d.** Phoom's verdict after real use: *"the summary UI feels incomplete and hard to use… it needs redesign."* Two concrete failures, both only visible on a record that is **still in use with expenses stacking up** — which is exactly the case the earlier design was never exercised against:

1. **"Settle up this record" is impractical at scale.** Ten "who pays who" rows is noise, not an answer. → the direct-pairwise sheet is **dropped**; consolidation becomes the only record-level settle surface.
2. **A summary didn't show what was already paid**, so a live record became confusing the moment new expenses arrived.

### 🔑 The one change that makes this better than what we built: THE FREEZE MOVES TO FIRST PAYMENT
The built model froze every share the instant you created a summary — which is why adding one expense afterwards forced a full revert. In the new model a group is **live and recomputing** until **someone marks a transfer paid**, and only then does it harden.

That trigger is the correct one, and it is the same instinct as toggle-in-place settling one level up: **a plan nobody has acted on is safe to recompute; the moment real money moves it must stop moving.** It removes the revert dance for the common case entirely. (The original rationale for freezing — that a share and the transfer replacing it must never both be live — is untouched: that ambiguity only matters once a transfer has been paid, which is precisely when the freeze now happens.)

### The interaction (all on the RECORDING DETAIL screen — no bottom sheet)
- **"Settle up this record"** no longer opens a sheet. It puts the log list into **check-mode**: a checkbox appears in front of every expense, **all ticked by default** (Phoom's call — the answer is "all of them" ~90% of the time; unticking is the escape hatch for "settle up week one, we're still going"), plus a check-all/clear toggle. The resulting **plan renders live as you tick**.
  - **Check-mode doubles as the record's "who owes who" view** — this is what replaces the dropped sheet. Tap Settle up → see the plan → Done, or back out. Looking and committing are the same gesture, so no separate list is needed and a never-grouped record is still readable.
- **Done** collapses the ticked expenses into one **group box** in the log. Two **independently collapsible** halves: **top = the summary** (the transfers), **bottom = the expenses it covers**.
- **While the group is LIVE (nothing paid):** the expenses inside stay fully editable, and **the summary recomputes on every change**. Add/edit/delete inside the group → the transfers update.
- **First transfer marked paid → the WHOLE group freezes** (all-or-nothing — the transfers are a joint net and cannot harden piecemeal). Its expenses become uneditable; their per-person settle boxes are gone; existing settled/open states are preserved exactly as they were.
- **Unfreeze = unchecking.** Clearing every settled transfer in a group unfreezes it and the expenses become editable again. Same rule as the old `canRevertSummary`, different trigger.
- **A frozen group is never added to.** New expenses form a new group. This is what keeps repeatability working.
- **Per-person settle boxes are hidden as soon as an expense joins a group — even a live one** (Phoom agreed). Mathematically they'd be harmless while live (the summary just recomputes), but they give two different ways to say "paid" for the same debt at the same time, which is the exact ambiguity that made minimized transfers untappable in Phase 6. Rule: **once grouped, the group is where you settle.** The expense still opens and reads normally; you just can't settle inside it.

### Log ordering (Phoom, 2026-08-08) — currently `ascending: true`, must flip
> *"when new expense come into log, put it on top. so old summary (which may be already clear) stay in bottom, user don't have to scroll to find new unsettled expense if there is 100 of expenses."*

Rule: **things that still need attention first, newest first within that; fully cleared things sink to the bottom, newest first among themselves.** An "attention" item = an ungrouped expense with any open share, or a group with any unpaid transfer. **Evie's call, easily reversed:** the same sink-when-cleared rule is applied to ungrouped settled expenses too, not just groups — one rule for the whole list reads better than a special case for groups. `RecordingDetail.jsx:55` orders `ascending: true` today.

### Group identity
A two-week record ends up as `[Group][Group][loose expenses]`, so "Summary 1" won't do. Groups get a **name auto-derived from the date range of the expenses they contain** ("Aug 1–7"), **editable**. Decided up front because it drives the group-header design.

### Data model — what changes from 5a
**5a survives almost entirely.** `settlement_events`, `summary_transfers`, `expense_item_members.summary_id`, `planSummary`, `buildTransferAtoms`, the net-preservation guarantees and all 42 tests stay. Two changes:
- **A group is a set of EXPENSES, and it persists while live.** Needs a group→expense link (a `summary_id`-style column on `expenses`, or a join table) plus a `name`. The old model had no membership concept — the group was implicitly "everything open at creation time."
- **Transfers are DERIVED while live and MATERIALIZED at freeze.** No `summary_transfers` rows exist until first payment; `planSummary` renders the live plan straight from the group's open shares. This keeps one source of truth and is what lets the summary recompute for free.
- So `createSummary` is re-pointed at **freeze** (materialize transfers with pinned amounts + stamp `summary_id` on the open shares) and `revertSummary` at **unfreeze**. Their write-order safety reasoning carries over unchanged.

### ✅ BUILT + LIVE-VERIFIED 2026-08-08 (Claude Code/Opus 5). `npm test` **154/154** (the original 136 green and untouched); build clean; **migration applied live**.

**Schema.** New `summary_groups` (`recording_id`, `name`, `frozen_at`, `freeze_event_id` → the settlement event created at freeze) + **`expenses.summary_group_id`**. Membership is by EXPENSE, not by share — a group is a thing you see in the log, so it has to survive an edit that rebuilds an expense's items. 5a's tables are untouched: `summary_transfers.summary_id` and `expense_item_members.summary_id` still point at `settlement_events`, so **every 5a mechanism (the freeze filter, transfer atoms, `canRevertSummary`) works unchanged** and any row written by the old flow stays correct. `superseded_by` is now always null (a frozen group is never added to) but is still honoured.

**Money core — nothing new was needed for correctness**, which is the sign the model was right. A live group's plan is just `planSummary` scoped to its expense ids. Two pure helpers added: **`groupDateName`** (→ "Aug 1–7", "Jul 28 – Aug 3") and **`sortLogEntries`** (the attention-first ordering). +18 tests, including the property that makes "live" safe: **a live group's plan is a pure function of its current expenses** — editing one inside moves the plan, and the ungrouped expense next to it is never swept in.

**IO (`balances.js`).** `loadGroups` / `createGroup` / `setGroupExpenses` / `renameGroup` / `ungroup` / **`freezeGroup`** / **`unfreezeGroup`**. Freeze and unfreeze wrap 5a's `createSummary` / `revertSummary`, so the write-order safety property (create the replacement first, freeze the originals last) still lives in exactly one place. `createSummary` now returns `{ summaryId, transfers }` — the caller needs the written rows to settle the one that triggered the freeze.

**`RecordingDetail.jsx` — rewritten.** Check-mode, the group box, freeze/unfreeze, rename, ungroup, the re-sorted log. Two incidental wins: its **load dropped from 3 phases to 1** (the record-scoped settlement load already carries items+members, so the two sequential queries for settled status are gone), and `ExpenseRow`/`TransferRow` are declared at **module scope** — defined inside the render body they'd be a new component type every render, rebuilding every row of a long log on any state change.

**`ExpenseDetail.jsx`.** Loads its expense's group. **Grouped (live or frozen) ⇒ the per-person settle checkboxes are gone** and the caption reads "settle in the group". **Frozen ⇒ Edit and Delete are gone too.** A banner states which it is, so the missing controls read as a rule and not a bug. `RecordSettleSheet.jsx` is **deleted** along with its App wiring.

**What was verified live** (isolated anonymous guest, a purpose-built *"Vietnam · month two"* record — 12 expenses over two weeks, 4 payers, VND under a THB era — i.e. the accumulation case that broke 5b): the log renders **newest-first**; check-mode opens with all 12 ticked and shows **3 payments where the old direct view showed 6**; unticking two expenses recomputes the plan live (`Done · 10`, amounts change); the dual-currency faded lines are right; no console errors; and the write-failure path genuinely surfaces `SaveError` (confirmed with a MutationObserver — it auto-dismisses after 4s, so a slow manual check misses it).

**✅ PHOOM-VERIFIED ON DEVICE (2026-08-09) — "summary function all visual check" passed.** The whole summary-group flow (check-mode → group box → freeze on first payment → unfreeze → the re-sorted log → "paid by" on every row) is accepted. §10.5R is DONE and deployed.

**✅ MIGRATION RUN + FULL LIFECYCLE VERIFIED LIVE (2026-08-08, isolated anonymous guest).** Walked the whole thing on the *"Vietnam · month two"* fixture:
- **Live group writes nothing.** After Done: the group row exists, `frozen_at` null, and **0 transfer rows, 0 closed shares, 0 events**. The plan is purely derived, as designed.
- **The live property holds.** Editing an expense inside (Ha Long boat ₫2.4M → ₫4.0M) moved the plan on reload — Sofia ₫1,294,167 → ₫1,827,500, Rui ₫274,167 → ₫807,500 — and **Ana correctly did not move**, because she wasn't on that expense. No revert needed, which is the entire point of the new freeze trigger.
- **Freeze on first payment.** Tapping a transfer → `frozen_at` set, 3 transfers materialized, **33 shares closed**, one `summary` event, tapped row ✓, expenses flip to LOCKED.
- **Unfreeze on clearing the last payment.** Transfers deleted, shares reopened, `frozen_at` null — and the event log reads **`summary, summary_reverted`**: append-only, nothing erased.
- **Nets preserved throughout.** After the full freeze → pay → unpay → unfreeze cycle, You ฿4,651.50 = Rui ฿1,130.50 + Sofia ฿2,558.50 + Ana ฿962.50, matching the plan totals exactly. Final state: sum of pairwise nets (฿1,883) **equals** You's global net, all nets sum to zero — the same diagnostic that exposed the C2 corruption.
- **Expense lockdown.** Live-grouped ⇒ banner "IN A SUMMARY", caption "SETTLE IN THE GROUP", no per-person checkboxes, Edit/Delete still present. Frozen ⇒ banner "LOCKED", **Edit and Delete gone**.
- **A frozen group is never added to** — a new expense ("Night bus") landed ungrouped, above the group, with the group showing "LOCKED · 2 UNPAID".

**⚠️ One anomaly, observed ONCE and not reproducible.** Immediately after the first unfreeze, both halves of the group box rendered collapsed (headers showed "3 PAYMENTS" / "12" but no rows); clicking a header restored them. Freeze and unfreeze were then run twice more, plus a transfer tap with both halves open, and it never recurred. Most likely cause is the **test harness, not the app**: opening the record detail used a chained `.click()` on an element *and two ancestors*, and React 18 flushes discrete events synchronously, so the 2nd/3rd clicks can land on freshly-rendered elements. Left unfixed deliberately — inventing a fix for a non-reproducible symptom risks adding a real bug. **If Phoom ever sees a group box render with counts but no rows, that's this; note what you tapped just before.**

### ✅ FOLLOW-UP — "paid by X" on every expense row (Phoom, 2026-08-08; built + verified same day)
Phoom: *"knowing who paid without having to get inside each expense help verify quickly if there are mistakes or not."* He suggested it sit behind OPEN/SETTLED; **it went in FRONT instead**, and the reason is the partial status: that label is variable-length (`2 of 4 · Sofia open`), so trailing it would bury the new information behind the longest string and add a third dot. Leading with the payer also puts every name at the same x-position down the column, which is what makes the list scannable. One-line flip if he disagrees.
- New `PaidBy` + `MetaDot` at module scope in `RecordingDetail.jsx`; applied to the loose feed, the rows inside a group box, and the check-mode list (that list is exactly where you're eyeballing for mistakes).
- **Just the name — no "paid by" label** (Phoom's call, same day): the position is consistent enough to be learned once, and the label cost three words on every row of a list whose whole job is being skimmable. Rows now read `SOFIA · 1 OF 3 · RUI OPEN`. Side effect: the shorter caption means the partial case no longer truncates inside a group box at 375px — the ellipsis fix below stays as insurance for longer names.
- **`SettledPill` is now shrinkable + ellipsized** (`minWidth: 0`, `overflow: hidden`, `textOverflow: ellipsis`). Caught by screenshotting at 375px: outside a group the combined line fits, but **inside a group box the card padding narrows the row and it hard-clipped mid-word**. Now the variable half yields first (`PAID BY SOFIA · 1 OF 3 · RUI …`) and the short fixed half always survives.
- **Defensive guard added to `ungroup()`**: it now refuses a frozen group. The UI can't ask today (the button only renders while live), but deleting a frozen one would strand its closed shares with no group left to unfreeze them — debts that exist and are invisible, the worst failure this model has. Guarded at the write rather than trusting future call sites.

**⚠️ TEST-HARNESS LESSON (this is what the "collapsed group box" anomaly actually was).** Driving this UI with `querySelectorAll(...).filter(...)` + `.click()` — especially parent-chains and `forEach` over multiple matches — fires **many unintended taps**. A guest account used for one afternoon accumulated 18 settlement events (9 freeze/unfreeze cycles), a junk expense titled with a timestamp, and an ungrouped group, none of it deliberate. Two takeaways: (1) prefer `read_page` refs + a single `computer` click, and re-seed a clean account before any visual judgement; (2) silver lining — that unintended churn was an accidental stress test, and the data stayed **perfectly coherent throughout** (0 orphaned transfers, 0 orphaned closed shares, nets summing to zero).

### Cost, stated honestly
5b's bottom sheet, preview-confirm dialog and button gate are **discarded** (~most of that unit). `RecordingDetail.jsx` becomes a substantially richer screen: check-mode, nested independently-collapsible groups, two freeze states, and a re-sorted log. **🔴 Difficult — build in Claude Code, not opencode**; this is where bugs hide, and it touches the settle path. `npm test` must stay ≥136/136 throughout.

   #### ✅ 5a — BUILT 2026-08-07 (Claude Code/Opus 5). `npm test` **136/136**, the original 94 green and untouched; build clean. ⚠️ **MIGRATION NOT YET RUN LIVE** — the paste block was handed to Phoom.
   Safe to deploy before the SQL runs: no summary rows exist ⇒ no share is frozen ⇒ every balance is byte-identical, and **the original 94 staying green IS that proof**.

   **Schema (`schema.sql`, + a migration paste block).** `settlement_events` (append-only log: `kind` ∈ summary | summary_reverted | settle_shares | unsettle_shares, `recording_id`, `ref_event_id`, `shares` jsonb, `note`; ⚠️ **nothing in it affects a balance — it is a log, keep it a log**) · `summary_transfers` (the live obligations: `summary_id`, denormalized `recording_id`, from/to, `amount`+`currency`, **pinned** `home_amount`+`home_currency`, `settled_at`, `superseded_by`) · **`expense_item_members.summary_id`** = the whole freeze mechanism. Old `settlements` table dropped along with the dead `recordPayment`/`deletePayment`. RLS is the same plain `owner_id = auth.uid()` check everywhere. Note: `settlement_events` is declared **before** `expense_item_members` in `schema.sql` because that table now points at it.

   **Money core (`balances-core.mjs`).** `buildContributions` skips a share whose group is fully `summary_id`-stamped — a third state, distinct from settled (nobody paid it; it was replaced). **🔑 The divisor is computed BEFORE that filter**, so closing one person's share can never enlarge what the others owe (asserted). New `buildTransferAtoms` turns unsettled, non-superseded transfers into **synthetic contributions** (`transferId` marker, `settleKeys: []`, alias-resolved), appended automatically by `buildContributions` — so `pairNet`, `pairNetGrouped`, `netByPerson`, `directTransfers` all work **unchanged**, and no screen can forget to include them. New `planSummary(contribs, scope, key)` → `{ transfers (each with `homeAmount`), shareKeys, transferIds, currency, homeCurrency, rate, count }`, writing nothing — it is what the preview-confirm renders. New `canRevertSummary(transfers, summaryId)`. `directTransfers` now also returns `transferIds` per row.

   **⚠️ ONE SPEC DEVIATION, deliberate — the home rate is BLENDED, not the record's `exchange_rate`.** The spec named the record's rate but also demanded "the record's home total must be identical before and after". Those conflict the moment two expenses in a record carry different pins: converting at the record's rate shifts the aggregate. `planSummary` therefore pins **one blended rate = the summary's total home ÷ its total base**, which keeps the aggregate exact, is still one explainable number, and **degenerates to exactly the record's own rate whenever every expense in it shares a pin** (the common case — asserted). Per-person home figures can still shift by a hair; their amounts in the settlement currency are exact.

   **IO (`balances.js`).** `loadSettlementData` supplies `transfers` in **both** paths (the scoped one filters on `summary_transfers.recording_id`, which is why that column is denormalized). New `createSummary` / `revertSummary` / `settleTransfer` / `unsettleTransfer` / `loadSummaries` — the money-critical writes live here so 5b is pure UI. **🔑 The write ORDER is the safety property:** create the replacement first, freeze the originals **last**, so a partial failure leaves both live (a visible double-count you can revert) rather than shares frozen with nothing replacing them (money silently gone). `revertSummary` mirrors it in reverse — unfreeze first. Fail loud, never quiet. If that ever proves insufficient the upgrade is a Postgres RPC, the same answer C2 needed.

   **Two consumer fixes made in 5a (found by grepping every contrib deref, not by guessing).** `Settlement.jsx`'s person sheet dereferenced `c.expense.title` / `c.item.is_rest` — a hard **crash** on a transfer atom. Transfer rows now render read-only ("Summary transfer · settle in the record"), are excluded from ticking (`tickable`), and keep a stable key — so the sheet still **adds up to the person card** but can't produce a tick that settles zero rows. EventSheet got matching `?.` (transfer atoms are never settled, so belt-and-braces).

   **🔴 FLAGGED IN-CODE FOR 5b — `RecordSettleSheet.jsx:63`.** Its local pair math has `paid = p.settled === p.total` and pushes only `settleKeys`. Once a summary exists, every share there is frozen and the only live debts are transfer atoms with EMPTY settleKeys, so `toggle`/`markAll` would settle nothing — the exact Phase-6 stuck state. A precise three-point comment sits above the code saying what to do (settle the pair's `transferId`s via `settleTransfer`/`unsettleTransfer` too; a settled transfer leaves the contribution set rather than flipping a flag, so it needs its own row state or a re-load). **Deliberately not fixed here** — it is 5b's design, not a latent bug today.

   ~~**Deliberately NOT built (5d's job):** `settleShares`/`unsettleShares` do not yet append events.~~ **✅ DONE in 5d — see §10.6 below.** The feared "`ownerId` threaded through every settle caller" churn never materialised: there are only **two** call sites, and `balances.js` reads the owner from the session already in memory, so no signature changed.

   **Tests (+42, all in one "SUMMARIES" block).** Phoom's 3-way cycle → exactly 2 transfers, both to A, C pays 200 / B pays 100; **every person's net unchanged across the summary** (the guarantee); closed shares produce no contributions and nothing is marked settled; within-record gross can only shrink and A becomes purely a creditor; a settled transfer and a superseded transfer both leave the open set; revert restores every debt exactly; **summary #2 sweeps the new share AND #1's two unpaid transfers** with nets still preserved; closing one share doesn't re-split the rest; all four `canRevertSummary` rules; single-payer record ⇒ plan == direct view; the KRW record blends to exactly 0.026 and its **total owed in home is identical after the summary**, transfers stay in the record's era; and the accepted **global gross-growth** case (summarising a record breaks a cross-record You↔Rui cancellation — every net preserved, documented rather than hidden).

6. **Notes, bank details, payment evidence** (his 2.6/2.7/2.8). **Needs Supabase Storage — not wired at all yet.** **Critique: attach bank details/QR to the PERSON (People tab), not the expense** — otherwise you re-enter Rui's QR on every expense he pays. 2.8 builds directly on unit 4's ledger.

---

## 10.6 ✅ UNIT 5d — THE AUDIT TRAIL IS VISIBLE (built + Claude-verified live 2026-08-09)

Closes the ask Phoom raised back in July 2026: *a durable log of every settle AND un-settle, plus a comment box on un-settle, so suspicious activity can be reviewed.* `npm test` **154/154** (untouched — this unit goes nowhere near the money core); build clean; **no migration needed**, `settlement_events` and every column it uses already existed.

The state it fixes: the table was live and already collecting `summary`/`summary_reverted` rows from every freeze/unfreeze — **and nothing in the app could show them**, while settling and un-settling wrote no event at all. A log with no writer for its two commonest actions and no reader for any of them.

**Part 1 — the writes (`balances.js`).** `settleShares(rows, at, note?)` and `unsettleShares(rows, note?)` each append one event after their share write succeeds. `rows` is already `[{itemId, personId}]` — the exact shape `shares` jsonb stores — so nothing is transformed. Two decisions worth keeping:
- **`ownerId` is read inside `balances.js`, not threaded through call sites.** `currentOwnerId()` pulls it from `supabase.auth.getSession()`, which resolves from memory/localStorage with no network hop. Zero signature churn at the two call sites, and a null owner skips the write rather than inserting an orphan row. 5a predicted this would be cross-cutting churn; it wasn't.
- **🔑 THE LOG WRITE IS FAIL-SOFT — deliberately the OPPOSITE of the summary write order.** By the time `logEvent` runs, the money write has already succeeded. Throwing would make the optimistic UI re-sync and show "couldn't save" for a settle that saved perfectly. A missing log row is a smaller harm than lying about money, so it `console.warn`s and returns. The rule to carry forward: **fail loud on money, fail soft on metadata.**
- `recording_id` stays null on share events (the shares resolve to their expenses client-side, so a lookup would be a wasted round-trip). New `loadEvents(limit = 200)`.

**Part 2 — the Activity section (`Settlement.jsx`).** A fourth History sub-section after Settled/Records/Expenses, rendering the log newest-first. Kind labels are colour-coded via a module-level `EVENT_KINDS`: SETTLED `--settled`, **REOPENED `--danger`** (a reversal must never read as a settle — same reasoning as the red Un-settle button), summaries neutral. Events are loaded in the screen's existing `Promise.all`, so no extra serial round-trip, and `background()` re-pulls just the log after each write — Activity stays current without re-deriving the whole money picture.
- **An event stores only `shares`, so what it COVERED is resolved client-side** against data the screen already has (items→expenses, recordings, people through the alias map). Renders "3 shares · Seoul dinner", "12 shares · 5 expenses", or the record name for a summary.
- **⚠️ Shares go stale and that is normal:** editing an expense deletes and rebuilds its items, so older events point at ids that no longer exist. Degrades to **"1 share · details no longer available"** — verified live by re-saving an expense and watching three events degrade cleanly, no "undefined", no crash.
- **🔴 ACTIVITY IS READ-ONLY ON PURPOSE.** Reversing a settle stays in the Settled section's sheet, so there is exactly one control surface for it. A log you can act on is how the Phase-6 stuck state happened.

**Part 3 — optional note on un-settle.** A `why? (optional)` input above the red Un-settle button in `EventSheet`, passed through to the log. **Only on that deliberate path** — the per-person toggle in an expense detail is a tap, and a prompt would ruin it; it still writes its event, just without a note. Group unfreeze likewise has none.

**Claude-verified live** (isolated anonymous guest — `is_anonymous` checked on the token first, per [[papaya-guest-verification]]; seeded demo data): settle from the person sheet → `SETTLED · 1 share · Airport taxi` appears immediately, no reload · un-settle from the Settled sheet with note "wrong person" → `REOPENED` carries the note **and the settle it reversed is still in the log** (Settled count drops to 00, Activity keeps both — exactly the July complaint, fixed) · per-person toggle in an expense detail → `SETTLED`, no note · summary freeze then unfreeze on *Seoul in June* → `SUMMARY · 6 shares · Seoul in June` then `SUMMARY REVERTED · Seoul in June` · **balances untouched throughout** (฿2,782 = 1,308.67 + 1,022.67 + 450.67 — the summary-equals-sum-of-cards diagnostic that caught the C2 corruption) · no console errors.

**Not built, deliberately:** `settle_shares` events carry no `recording_id`, so Activity can't yet be filtered to one record. Trivially addable if Phoom wants it; unasked-for today.

---

## 10.7 🟡 UNIT 6 — NOTES + HOW-TO-PAY (built 2026-08-09, ⛔ **BLOCKED ON A MIGRATION — DO NOT PUSH YET**)

**Read this before touching the branch.** Commit `a4ec03a` is on `papaya-v2` **locally and deliberately NOT pushed**. Unlike every previous unit, its migration is **not** a provable no-op: the code writes columns that don't exist yet, so pushing it before the SQL runs would make **saving an expense and editing a person fail outright** on the live app. Confirmed live, not assumed — a save with the code in place returns `400` from PostgREST.

**Run this first (Phoom, in the Supabase SQL editor), then push:**
```sql
alter table expenses add column if not exists note text;
alter table people   add column if not exists payment_note text;
```
Both are purely additive, null for every existing row, and read by nothing in the money core — so they move no balance. `schema.sql` already carries them.

**6a — a note on an expense** (his 2.6). A `Note` row in `ExpenseForm` directly under `Title`, and a wrapped, full-width `Note` block on `ExpenseDetail` between the meta rows and "Made up of". Answers "what was this ฿400 for?" months later. Kept as an inline row rather than a textarea so the form keeps its dense rhythm; long notes truncate in the form and read in full on the detail. **⚠️ `ExpenseForm` already had a `note` state — a transient alert message.** The expense's own note is therefore `expenseNote`; don't conflate them. It's in the dirty-check signature, so typing a note and backing out prompts the discard confirm.

**6b — how to pay a person** (his 2.7, text half). `people.payment_note` — free text for an account number or PromptPay id. **Deliberately on the PERSON, not the expense**, per the standing critique: otherwise you re-enter Rui's details every time he pays for something. Edited in the shared `EditSheet` (so it reaches People **and** PersonDetail, including self), **hidden in the "Add person" flow** so adding stays one name and one tap. Displayed in `PersonDetail`, and — the part that earns it — in the **Settlement person sheet at the moment of need**, shown only when the balance runs your way. That panel keys off the whole balance, **not** `remaining`, or ticking everything would hide the details exactly as you go to pay. Both display sites are `user-select: text`, since the entire point is copying it into a banking app.

**⚠️ Not built — 6c, the photo half** (QR image, payment evidence). **Supabase Storage is not provisioned** and cannot be from here: creating a bucket needs dashboard/service-role access, and the repo holds only the anon key. Building upload code blind, against a bucket that doesn't exist, on an app in real use, was not worth it. See the handoff for the setup block and the design when Phoom wants it.

### 🔴 Bug found and fixed on the way (`ExpenseForm.save()` had NO error handling)
Tripped over while testing the above: the 400 threw straight out of the promise, so `saving` was never cleared and **Save simply went dead with nothing on screen**. Pre-existing, and it matters in real use — §2b already records that there is no offline support, so *any* lost-signal save hit this. Now the body is wrapped in `try/catch`, the insert's `error`/null result is checked explicitly, and a failure surfaces through the form's existing alert: *"Couldn't save this expense. Check your connection and try again — nothing you typed has been lost."* The form keeps everything typed and Save can be pressed again. **Verified live** using the missing column as a guaranteed failure — a happy accident of the blocked migration.

This fix needs no migration and is independently valuable, but it shares `ExpenseForm.jsx` with 6a, so it is **in the same unpushed commit**. Splitting it out would have meant a hand-rolled partial-stage on a production branch with Phoom away — not worth the risk for a few hours.
