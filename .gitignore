node_modules
dist
# every env file EXCEPT the committed template. A sandbox/staging project's
# keys must never land in the repo, and `.env` + `.env.local` alone did not
# cover `.env.sandbox`.
.env
.env.*
!.env.example
.DS_Store
*.log
.claude/settings.local.json

# raw design-tool exports (key deliverables live in design/)
Expense Form Design System/
Papaya home screen restyle/
style-refs/
